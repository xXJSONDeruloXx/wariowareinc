# Decomp pattern library

## Build / verification rule (critical)
- **Widened-parameter narrowing order**: when a target truncates its second/third incoming registers with explicit `LSLS`/`LSRS`/`ASRS` pairs at specific points between other statements, declare those parameters as wide `s32` and perform `(u16)`/`(s16)` conversions as standalone locals in exactly the target's statement order. Narrow declared parameter types let the compiler normalize at entry in declaration order, which mismatches when the target interleaves the narrowings around an operation-flag store. `sprite_set_z` is the exact ordinary-C example.
- As of the strict real-C decomp guard, new `src/decomp/*.c` progress may not use original-asm wrappers, instruction-bearing inline asm, empty asm barriers, compiler register pins, non-mapped `volatile`, or opaque offset-heavy byte-pointer stand-ins. Historical notes below mention those legacy techniques; do not use them for new conversions. Bounded raw pointer casts and numeric offsets remain allowed but are recorded by `tools/audit_decomp_source.py`; the audit follows scalar-pointer aliases across later lines, and use a named overlay when a candidate touches several fields in one packed record.
- Always verify with Docker before committing: `docker run --rm -v $(pwd):/workspace devkitpro/devkitarm:latest /bin/bash -c "cd /workspace && make -j4"`
- The local `tools/agbcc/bin/agbcc` is not a reliable macOS host-native path; Docker is the only supported local verification path for this repo
- A chunk is not done until `wariowareinc.gba: OK` is confirmed in the Docker build output
- `make report` must be run (via Docker) after the ROM build before completing the chunk — this regenerates `build/report.json` which is needed for accurate matched-function / matched-code tracking
- Convenience commands: `/decomp-verify` runs clean Docker build + Docker report + objdiff refresh; `/decomp-report` runs Docker report + objdiff refresh
- `python3 tools/gen_objdiff.py` tracks **linked** unit coverage only; `included_stub` conversions do not increase linked C TU counts
- count `src/decomp/*.c` separately when you want total decompiled function-file coverage (`standalone_tu` + `included_stub`)
- Apply preflight now rejects a candidate that references a `D_XXXXXXXX` symbol absent from `undefined_syms.ld`; isolation can link unresolved C symbols for byte comparison, but the full ROM linker cannot.
- **Main-menu update branch shape**: when the target calls `beatscript_scene_is_inactive`, branches with `BNE` to a one-call/`return 1` path, and leaves the three housekeeping calls on the fall-through path before `return 0`, write the ordinary C early path first (`if (inactive != 0) { ... return 1; }`) and put the fall-through calls in a block. The semantically equivalent `if (inactive == 0) { ... return 0; }` spelling generated the opposite branch and a real near miss in `func_08016D00`.
- **Canonical ROM data symbols**: if `include/undefined_syms.inc` already names a ROM object such as `D_083FBB44`, add the same address to `undefined_syms.ld` before applying a standalone C candidate and keep the C declaration typed (`extern struct SongHeader D_083FBB44;` plus `&D_083FBB44`). This preserves a named literal-pool reference and avoids a magic numeric address fallback.
- **Strict real-C evidence**: `tools/audit_decomp_source.py --strict` is now an independent admission gate. A candidate may show raw pointer casts/offsets for a not-yet-recovered packed layout, but it must contain no original-asm wrapper, instruction asm, empty barrier, or compiler register pin. Batch 229's three scene accesses are recorded as evidence and still pass this gate.
- **Sentinel-table walker with `LDM Rn!`**: for a loop that tests a word at the current cursor, then loads that same word while post-incrementing the pointer before a helper call, use a typed `const u32 *cursor`, separate `while (*cursor != 0)` test, `value = *cursor`, and `cursor++`. m2c may render scalar-pointer `+= 4`; on an `s32 *` that means 16 bytes in C and is not the target `LDM` stride. `func_0800C704` and `func_0800C720` are exact ordinary-C examples with no numeric pointer offsets.
- **Padded byte-table entry stride**: when a loop stores one byte and advances by `0x20`, model the entry as a named record (`u8 value; u8 pad[0x1F];`) and advance a pointer to that record with `entry++`. Pair it with a small named root overlay for the table pointer when the root field is still unnamed. `func_08022070` is exact; the semantically equivalent raw `u8 * += 0x20` spelling remains unnecessary layout debt.
- **Named fixed-stride record walk**: when a helper is called twice on records `0x40` bytes apart, a named padded entry type plus `entry++` preserves the stride and keeps the source semantic without a scalar pointer increment. `func_08089148` is the exact example.
- **Fall-through threshold branch**: when the target branches on `value > 0x80` to the small return and leaves the larger default constant as fall-through, write `if (value > 0x80) return 2; return 0x1A;`. The equivalent `if (value <= 0x80) return 0x1A; return 2;` reverses branch opcode/constant layout. `func_080D6D28` is the exact example.
- **Small named record counter overlay**: when a helper increments the word at record offset `0x34`, stores it, and compares it to the word at `0x30` before a reset helper, use a named overlay with leading padding. This preserves layout evidence and ordinary C without an offset-heavy pointer blob; `func_08064D10` is the exact example.
- **Returned five-halfword allocation record**: for an allocator wrapper that fills a 0xC-byte record with one helper result, two `0x100` defaults, and zeroed halfwords, use a named five-`u16` record, assign fields in target order, and return the typed pointer. `func_0800C73C` is the exact ordinary-C example.
- **Narrow callback ABI in a wrapper**: when assembly truncates the third incoming register with `LSLS`/`LSRS` before a helper call, use a `u16` parameter and retain the direct typed value through the call. `func_080DF224` matches with `s32 *arg0, s32 arg1, u16 arg2`; widening the parameter and casting only at the call site changes the code shape.
- **Load-order local for fixed-point helpers**: when the target loads a base field before conditionally correcting a signed fixed-point field, load both into named locals before the `if`. `func_080CF440` and `func_080CF6C0` match this ordinary-C form; the semantically equivalent late base-field read changes the target register/load order.
- **Target-neighborhood ABI declaration**: a caller may need the target neighborhood's widened declaration (`s32 get_random_range(u16)`) to preserve an unnormalized return register even when the shared implementation/header currently says `u16`. Keep this visible as an ABI provenance item and do not hide it behind casts or asm; `func_0801CB24` records the remaining project-wide prototype-cleanup debt.
- **No codegen-forcing volatile**: the strict audit rejects ordinary C `volatile` unless it is a direct access to a fixed GBA mapped-memory address (`0x02000000`–`0x07FFFFFF` or `0x0E000000`–`0x0E00FFFF`). This keeps legitimate hardware/shared-memory semantics available while preventing a volatile local/pointer from being used as an instruction-order shim. The audit records the offending line and the isolation/apply paths reject it before Docker comparison.
- **Opaque layout guard**: `--strict-layout` and the Git hooks reject a new candidate with more than two raw scalar pointer-offset access lines unless it also exposes a named struct/field model. This keeps byte-accurate packed-layout evidence available while preventing a large `u8 *base`/numeric-offset blob from serving as a semantic substitute for decompilation. Existing raw-layout matches remain reviewable historical work until their records are recovered.
- **Alias-aware offset guard**: the layout count includes later uses such as `u8 *p = (u8 *)scene; p[0x10] = ...;`. A cast on the declaration line is not a way around the offset budget; use a real project type or a small named overlay when the layout is understood.
- **Overlay provenance matters**: an unrelated dummy `struct` declaration/field access does not excuse a separate offset-heavy byte-pointer blob. The audit requires named-overlay evidence to stay bounded too; a named overlay should model the fields actually used by the candidate.
- **Named overlay with preserved gaps**: Round 85's `func_0801743C` needed a previously unnamed halfword at graphics-buffer offset `0x48`, while the known `unk4C` field stays at `0x4C`. A small `GraphicsInitRegisters` overlay with explicit padding modeled both fields and compiled exact; naming the fields is preferable to writing `*(u16 *)((u8 *)&gGraphicsBuffer + 0x48)` in a new candidate.
- **Base-zero scene-variable store**: for a word written at offset zero of `gCurrentSceneVariable`, an inline named root overlay (`((struct SceneVariableRoot *)gCurrentSceneVariable)->value = ...`) preserved the target's post-call global reload. Keeping the overlay as a local lvalue avoided the extra callee-saved pointer register emitted by a local pointer variable in `func_080179E4`; it is bounded layout evidence, not an opaque pointer blob.
- **Title-scene loader fan-in**: the same offset-zero `SceneVariableRoot` overlay transfers to a setup sequence that calls `func_0800A3A4(1)`, `func_08006A04()`, `func_08006B90(0)`, stores `func_080042F4(...)`, then clears/finishes the scene. `func_080178C4` is the exact ordinary-C example; keep the overlay named and do not pad its frame when a sibling with a larger target stack reservation remains a near miss.
- **Named overlays for small packed-record leaves**: Round 87's exact `func_0807DC6C`, `func_0808EF04`, `func_080D6C30`, `func_080526EC`, `func_08086970`, `func_08025174`, `func_080D70EC`, and `func_0808828C` candidates use named fields plus explicit preserved gaps. The strict audit sees typed field access and no raw scalar-pointer offsets; this is the preferred middle ground when a complete project-wide record type is not yet recovered.
- **Sibling fixed-point accumulators**: `func_080526EC` and `func_08086970` both load the named `gCurrentSceneData` halfword before multiplying it by itself and adding a shifted result into a scene-variable accumulator. Keep the two records as small named overlays, preserve the `u16` temporary and statement order, and let the compiler comparison—not a cast or barrier—select the exact spelling.
- **Named table pair with indexed fields**: `func_08025174` models the table at `D_03006524` as a named record with two two-entry word arrays. This keeps the two indexed stores readable and avoids replacing the table with an offset-heavy byte-pointer view.
- **Direct typed global reloads**: when a target reloads `gCurrentSceneData` between sibling calls, use the named project macro/struct field directly (`gGameplayData.unk278`) rather than caching the base in a local pointer. `func_0801709C` matched only after that source-shape correction; the local pointer was semantically correct but changed the saved-register and load sequence.
- **Packed-address flag wrappers remain evidence-only when codegen disagrees**: m2c's `arg0 & 0x7FFFFFFF` plus high-bit DMA skeleton for `func_0801711C`/`08017164`/`080171AC` was ordinary C and passed the layout gate, but agbcc collapsed the high-bit test to a signed branch and changed the source/destination register order. Do not force this with a mismatched prototype, repeated casts, barriers, or instruction asm; retain the near-miss receipt until a legitimate type/source spelling reproduces the bytes.
- **MOVS+RSBS mask folding remains a real compiler trap**: `func_080174A4`'s clear-bit source was semantically correct but agbcc folded `MOVS #2; RSBS` into an immediate `0xFE` mask. A near miss is useful provenance; it is not permission to add a volatile barrier or inline instruction sequence.
- **Sentinel `-1` literal-pool trap**: in `func_0800C6E4`, the readable typed-store candidate `(u16)-1` caused a literal-pool load, while the target uses `MOVS #1; RSBS`; a signed local improved the source hypothesis but still did not reproduce the full loop. Keep the pointer-list candidate evidence-only until a natural ordinary-C spelling matches.
- **Named bit-table overlays do not waive register/mask traps**: the `func_080F0E9C`/`080F0EBC`/`080F253C`/`080F2598`/`080F25B8` screens used named 0x20-byte entries and passed layout quality, but agbcc still selected different register ownership and folded masks. Preserve these as near-miss evidence rather than adding pins, barriers, or raw offsets.
- **Scene-init pointer stores**: a short initializer that writes an allocated ID at scene offset `0` and clears the byte at `+4` can remain ordinary C with explicit typed stores when those offsets are the only unresolved layout. `func_08016F14` matched this form without pins or barriers; keep the offset evidence visible until the scene record is named.
- **State-machine and repeated-store near misses stay evidence-only**: semantically correct C for a multi-state helper may still choose a different switch/branch ladder, and a sequence of four packed scene stores may choose the opposite global-base register. `func_08016DE0` and `func_08016C24` remain near misses from Batch 231; do not solve either with compiler register pins under the strict real-C rule.
- **Scene/main-menu state dispatch**: a `u16` read from the current-scene record at `+0x3A`, followed by cases `0` and `1` and a common key-update tail, matches as an ordinary C `switch` when the already-converted leaf helpers are declared explicitly. `func_08016808` is the exact example; the `+0x3A` access remains visible audit evidence until the scene record is named.
- **Beatscript bootstrap stack shape**: when a bootstrap passes one subscene pointer followed by a null terminator to `set_beatscript_subscenes`, model the local pointer array at the target's full four-entry size. `func_08016CBC` matched with `const struct SubScene *subScenes[4]`, ordinary assignments, and no asm or register metadata; a two-entry array preserved bytes except for the target's larger stack frame.
- **Bitfield-to-thread state leaf**: use the typed `gGraphicsBuffer.unk854_4` bitfield and `gBeatscriptScene.threads[0].active` field for the sound-stop/scene-thread flag helper. `func_080167D4` reproduces the target's load/mask/store with named project types and no raw pointer access.
- **Graphics update with packed scene fields**: when a graphics register field is named but the scene record is not, keep the `gGraphicsBuffer` field access typed and spell each unresolved scene halfword/word offset explicitly. `func_08016C60` is exact ordinary C; its five scene offsets are recorded by the strict audit as layout evidence rather than hidden in asm.
- **Included-stub isolated BL normalization**: when a candidate is compared against a whole host-TU object, import the host object's section-relative `T/R/D/B` symbol offsets into the candidate link. Otherwise every external call can appear as a false `BL 0` mismatch even when the function bytes are exact. Keep the entry function excluded from that map so the candidate remains rooted at offset zero.
- **Embedded asm label transport**: C-string asm stores labels as lines ending in `\\n\\`; strip that transport suffix before parsing labels/directives for m2c/asmlift. Parsing only address-commented instructions drops branch targets and creates a false decompiler failure.
- **Included-stub apply representation**: screen the raw C body as a standalone object, then wrap it at apply time with `#if __INCLUDE_LEVEL__ > 0` / `#endif`. The Makefile compiles `src/decomp/*.c` separately while the host TU also textually includes the same file; omitting the guard creates a duplicate definition. The guard is a build-integration wrapper, not an instruction-matching candidate change.
- **Included-stub external-call relocation false near miss**: normalized isolation can remain at 99.59–99.72% when the candidate's external `BL` relocation records differ even though direct instructions, host-TU placement, and final ROM bytes match. For included stubs, use the full host-TU Docker gate as the final authority and record the relocation-only score in the receipt; keep standalone candidates exact-only. Included stubs also bypass standalone `D_XXXXXXXX` map validation because their host TU supplies the symbol environment.
- **agbcc requires `-mthumb-interwork`** to generate interwork-safe epilogues (`POP {R0}; BX R0` or `POP {R1}; BX R1`). Without it, agbcc generates `POP {PC}` which is NOT interwork-safe and does NOT match the original. The Makefile passes `-mthumb-interwork` via `CFLAGS`. When doing isolated agbcc testing, always include this flag.

## Proven high-yield families
### Easy filler / utility
- standalone `BX LR` leaves (simple `void func(void) {}` matches in final linked ROM despite object-level NOP padding diff 0x0000 vs 0xC046)
- **Explicit zero padding after a standalone function**: if the source asm has a literal `.short 0x0000` before alignment, add `__attribute__((section(".text"))) const u16 _padding_<address> = 0;` after the C body. This restores the ROM bytes; the standalone report may count the padding symbol as an additional matched text symbol.
- simple tail-call wrappers
- 3-word struct stores: `a0[0] = a1; a0[1] = a2; a0[2] = a3;` — agbcc generates separate STR instructions (matching the original), while devkitARM gcc uses STMIA. **Must verify with the agbcc toolchain or the full Docker build, not devkitARM gcc alone**
- simple void-call wrappers
- `MOVS R0, #const; BX LR` return-constant helpers
- zero-arg wrappers: `func(arg0, arg1, 0, 0)` with non-void return type to get `POP {R1}; BX R1` epilogue instead of `POP {R0}; BX R0`
- **Non-void no-return epilogue shaping**: when the target ends `POP {R1}; BX R1` but leaves the result register untouched, declare the C function with a scalar return type and omit the explicit `return`. This preserved the ABI epilogue for `func_080042F4`, `func_080049A4`, and `func_08007FC0` while remaining ordinary C.
- **Ignored stack slots are part of the ABI shape**: if a function loads later stack arguments while skipping earlier slots, model the unused parameters in the C prototype. `func_080043B8` needed eight formal parameters so the two stored values compiled from `SP+0x14` and `SP+0x18`; omitting the unused slots shifted both loads.
- **Stage arithmetic when register reuse matters**: a mathematically equivalent expression can reassociate multiplies and change Thumb register operands. In `func_080F2FFC`, a first `value = arg1 * arg0` followed by `value *= arg2` reproduced the target's `MUL R0,R1` then `MUL R0,R2` sequence.
- **Pin loads when literal order is semantically visible to the matcher**: register-bound C locals can preserve a target's global-literal load order without an instruction asm block. `func_0801E44C` pins the data base to `R1` and sprite handler to `R0`; `func_08007FC0` pins the returned allocation and tail pointer to the target registers.
- **Heap-copy return-register shaping**: for a target that copies through the allocation pointer in `R0` and exits with `POP {R1}; BX R1`, pin the destination pointer to `R0` in the ordinary C spelling. Without that declaration, a non-void no-return function may move the destination to `R2` to preserve `R0` as a conceptual result; `func_08004BD4` and `func_0800557C` use the pinned form.
- **DMA sibling family**: wrappers around `dma3_set`/`dma3_fill` generally match directly when they include `src/code_08000f10.h` and pass the mapped absolute destination as a byte pointer. Before applying, mirror every `D_0300XXXX`/`D_08XXXXXX` symbol used by the C candidate into `undefined_syms.ld` if it is only present in `include/undefined_syms.inc`; the linker preflight catches this before the full ROM build.
- **DMA-table initializer ordering**: for a table setup that stores a source pointer, destination, and control word through a register-pinned `volatile u32 *` base, delay the `R3` table-symbol load until after the first store and retain the target's post-control-word volatile read (`r0 = base[2]`). This preserves the target's `STR R1` then `LDR R3` order in `func_08002620` and `func_0800774C` without instruction asm.
- **Scene-table wrapper fan-in**: small scene loaders that call `func_0800EB50` and then pass `*(u32 *)((u8 *)gCurrentSceneData + 8)` plus a ROM table address match with `#include "scenes.h"`, `extern u8 D_083A98xx[]`, and an explicit `func_0800C704/func_0800C720(u32, const void *)` prototype. Keep the table symbols as arrays so the call emits the target address literal; add their canonical linker assignments before apply.
- **Table-entry load with `ADD R0,R4`**: for random music-table siblings that load a word from `base + ((random << 16) >> 14)` after a call, bind the table base to `R4`, bind the offset accumulator to `R0`, and use `offset += (u32)base` before dereferencing. Direct pointer addition produces the three-operand `ADD R0,R4,R0`; the staged accumulator reproduces the target two-operand add.
- **Graphics-buffer indexed offset accumulator**: when a target does `LSLS R0,#16; LDR R2,=gGraphicsBuffer; LSRS R0,#15; ADDS R2,#0x54; ADDS R0,R2`, use register-bound `R2` base and `R0` offset locals, assign the base between the two shifts, then add the base to the offset. Direct `&gGraphicsBuffer + 0x54 + offset` folds the offset into the literal or emits a three-operand add.
- **Post-call `gCurrentKeys` test siblings**: after one or more void calls, `func_08009EE4(((u16)gCurrentKeys >> 8) & 1)` with explicit `extern u16 gCurrentKeys` and `extern void func_08009EE4(u32)` declarations reproduces the `LDRH; LSRS; MOVS #1; ANDS` tail. This transfers directly across same-shaped scene wrappers.
- **Scheduler callback-plus-one wrapper**: a `u16` `get_current_mem_id()` declaration, a typed `schedule_function_call(u16, void *, s32, u32)` declaration, and `(void *)((u8 *)&func_08016EC8 + 1)` preserve the target callback literal and the `LSLS`/`LSRS` normalization in `func_08016EF8`.
- **MOVS+RSBS mask folding remains blocked**: for `func_0800CDB0`, a local absolute base pointer recovered the target's separate `LDR [base,#2]`/`STRB [base,#2]` shape, but pure C still folded `3; 0 - 3` into `0xFD`. Preserve the near-miss receipt and do not reintroduce instruction-bearing asm solely to force the two-instruction mask.
- pointer-deref halfword store: `*(short *)((int *)a0[3]) = -1;` (generates LDR R1,[R0,#0xC]; MOVS R2,#1; RSBS R2,R2,#0; ADDS R0,R2,#0; STRH R0,[R1]; BX LR)
- conditional-call wrappers: `if (func_08011698()) callee()` — void function with CMP+BEQ+BL pattern
- const-arg sequential calls: `func_0800C7A4(8); func_0800C7A4(9)` — each call resets R0 before BL
- two sequential void calls: `callee1(); callee2();` with PUSH {LR}/POP {R0};BX R0
- short standalone wrapper siblings: direct C field stores/calls, explicit `u32` loop counters for `BLS`-shaped bounded loops, and byte-pointer offsets can reproduce whole 18–30-byte Thumb wrappers without register pins. Batch 185 matched ten such functions in one fan-in transaction; `func_080043A0` is the recorded raw-object fallback when target symbol metadata prevents linked isolation.
- gGraphicsBuffer.DISPCNT bit-OR-set: `gGraphicsBuffer.DISPCNT |= (0x80 << 1) << arg0` — generates LDR+MOVS+LSLS+LSLS+LDRH+ORRS+STRH

### gCurrentSceneVariable / gCSV families
- direct byte/word setters
- pointer-deref helpers
- shift-based byte setters
- `((u8*)gCurrentSceneVariable)[N]++` / `--` siblings
- shift-computed offset loads/stores like `(0xBD << 4)`
- `sprite_id_delete(gSpriteHandler, *(u32 *)((u8 *)gCurrentSceneVariable + off))` siblings
- signed-load helpers using explicit `(s8)` or `(s16)` shaping
- multi-store patterns that need a local pointer reload shape

### Wrapper families
- `scene_set_current_thread(1)` + byte/word store wrappers
- two-call and four-call R4-save wrappers
- wrappers that preserve `arg0` in `R4` across multiple BLs
- const-arg BL wrappers once literal-pool behavior is known-good
- two-pointer wrappers passing `p + off1`, `p + off2`
- **Callback/VRAM wrapper spellings**: `func_0800D23C` matches with a typed `void *` callback argument `(void *)((u32)func_0800CFFC + 1)` and a `u32` constant; `func_08019A8C` matches with a typed `void *` `VRAMBase + 0x8000` argument followed by `func_0800BF0C(0)`. Keep the callback and absolute-address constants as ordinary C expressions and verify the literal-pool shape in isolation.
- **Three-argument sprite callback wrappers**: for callback entries that ignore R0 but consume the sprite ID in R1 and sound/data in R2, retain an unused first parameter, use a widened `u32 id` plus `s16 sprite_id = (s16)(u16)id`, and call `play_sound(sound)` before the sprite callback setters. When `sprite_set_callback`'s fourth parameter is an integer ABI slot, `(u32)&D_083FF654`/`(u32)&D_083FF67C` preserves the named literal-pool word without inline asm. This matched the `08016B4C`/`08016B88`/`08016BC4` sibling trio with ordinary C only.

### Data-structure / arithmetic families
- raw-pointer struct-entry setters
- pair-add / arithmetic helpers
- small struct-init / zero-init functions
- **Pure arithmetic/packing leaves**: explicit byte casts and shift temporaries can reproduce compact leaf helpers without barriers. The big-endian four-byte reader in `func_080F2C50` matches with sequential `u8` loads and explicit shifts/ORs; the signed-offset stores in `func_0803F224`, `func_0803F26C`, and `func_0806754C` match with direct `s32` shifts and indexed stores. For `func_080F1F9C`, test the upper-bound branch first and cast the doubled value back to `u8` to retain the target shift-pair. For `func_080F28F8`, a register-bound C temporary preserves the target copy-then-shift instruction order.
- **Standalone C alignment contract**: isolated agbcc candidates must append the same zero-filled `.text` alignment tail as the Makefile. Otherwise an object ending at offset `2 mod 4` receives a linker NOP instead of the target's explicit zero halfword and appears to be a false near miss.
- **Malformed or locally truncated target symbol metadata**: if a legacy asm file places `.thumb_func` after `glabel`, or a linked target symbol ends at an internal local label, normalized linked-ELF objdiff can fail or report a misleading partial match even when the full instruction bytes match. Treat the normal standalone path as exact-only. A narrowly documented `apply-batch --force` exception is acceptable only when the receipt explains the target-symbol boundary, the complete object bytes are accounted for, and the clean full-ROM gate passes; never use it to waive a compiler instruction difference.
- **Compiler literal-pool symbol boundary**: a standalone candidate can have an instruction-identical body and identical literal-pool bytes while normalized objdiff still reports a near miss because the compiler function symbol extends through its pool and the target's local pool label ends the function earlier. Round 54's `func_08002620` and `func_0800774C` are the concrete DMA examples. Keep the default exact-only rule; use the narrow force exception only after accounting for the full code/pool bytes and passing the clean full-ROM gate.
- **Short soundplayer wrapper normalization**: for wrappers that receive a widened raw argument but pass a `u16`/`s8` value to an existing soundplayer helper, keep the raw value in a `u32` temporary, perform the target's `LSLS` normalization before the null check when present, and stage the later sign extension separately. `func_08002038`, `func_0800207C`, and `func_080020E0` reproduce the target `LSLS`/`LSRS`/literal-pool shapes with ordinary C; an explicit `(u32)0xFFFF` argument preserves the E0 literal load.
- **Named soundplayer symbol boundary audit**: `set_soundplayer_pitch` and `set_soundplayer_volume` use the same widened-argument normalization, but their descriptive raw symbols expose a legacy local-label size boundary to linked objdiff. Keep the ordinary-C candidate, compare the complete `.text` section bytes, and require the full ROM gate before using the metadata-only exception; do not treat the linked score alone as a compiler mismatch.
- **Complete linked-text audit for locally bounded symbols**: when normalized objdiff stops a legacy target at an internal return or literal-pool label, extract the entire linked `.text` section after the cycle's zero-filled `.align 2, 0` normalization and compare SHA-256 plus size. Round 62 proved 20/20/28/44-byte equality for the three soundplayer wrappers and `func_08006148`; the force path waived only symbol coverage, never an instruction byte.
- **Round 55 table-copy boundary**: the five `func_08002FC0`/`08002FE8`/`08003028`/`08003040`/`08003058` candidates had direct `.text` byte equality but normalized candidate symbols extended through the target's internal local-label boundary. The direct byte receipt plus full ROM gate made them valid metadata-only force cases. `func_08007AD4` is the contrasting trap: a same-sized candidate still had two actual prologue bytes in the wrong order and was correctly rejected.
- **Absolute ROM global pointer setter**: when a target loads a pointer-valued `D_08XXXXXX` symbol through a literal pool and then writes regular word fields, declare it as `extern u32 *D_08XXXXXX`, assign a local `u32 *base`, and store through sequential indices. `func_08024E34` reproduced the target `LDR literal; LDR pointer; STR` sequence and required the matching canonical assignment in `undefined_syms.ld` even though `include/undefined_syms.inc` already knew the address.
- **Task-pool slot scan/cancel**: for the `D_030006A0` table, use a raw byte pointer with `0x1C`-byte advances and explicit offset accesses (`u8` flags at `+0`, `u16` ID/state fields at `+2`/`+0`, and `s32` owner at `+8`). Register-bound `u32` locals plus goto-shaped labels preserve the `BGE` entry, the backward `R2`/`R4` loop, and the `R5`/`R6` mask roles in `func_08005920`; split the raw `LSLS` normalization from the later `LSRS` assignment so the task-ID local naturally occupies saved `R7` in `func_080059E4`. Add the canonical `D_030006A0` linker assignment before a standalone conversion. The Round 58 linked byte receipt is required because these legacy targets' local literal-pool symbols do not cover the full linked text range.
- **Task-pool mutation siblings**: continue the same raw-pointer form for `func_08005834`/`08005870` owner scans, `func_080058AC` active-owner cancellation, `func_080058DC` state-mask setting, `func_080059A8` field-`0x18` update, and `func_08005A54` ID/state mutation. Keep the active flag at `+0`, owner at `+8`, ID at `+2`, and stride at `0x1C`; explicit register-bound locals preserve the different `task_stop` mode constants and loop counters. Do not force an overlapping C struct model when byte/halfword fields are aliased by the target. Round 59 confirmed that zero-filled `.align 2, 0` candidate normalization removes false tail near misses, but the target symbol-boundary issue still requires a complete linked-text byte receipt before the narrow metadata-only force path.
- **Small wrapper/allocator family**: `func_0800200C` is a nested zero/nonzero dispatcher whose direct calls to already-converted C wrappers must still be checked in full context; `func_080041B4` is a byte flag test against `D_03000684` followed by a zero-argument call, and requires the canonical linker assignment when converted as a standalone TU. `func_08005F64` allocates a small record with two `func_08006184` calls, normalizes the first argument through `LSLS/LSRS #16`, and stores the returned pointer plus two halfwords. Register-bound `R4/R5/R6/R8` locals and one empty `"+r"` compiler constraint preserve the target's initial copy ordering; the constraint emits no instruction and is not a substitute for real C. Round 61 proved the three complete linked sections byte-equal before the strict-ROM transaction.
- **Heap-record cleanup wrapper**: for a record whose owned pointers are at byte offsets `+8` and `+0xC`, use a byte-pointer view, call `mem_heap_dealloc` in target order for both fields, then pass the original record to the final call. A local `extern void mem_heap_dealloc(void *)` can avoid the conflicting `memory_heap.h` declaration when the standalone TU is screened in isolation; the clean link remains authoritative.
- **Scene-variable table lookup accumulator**: for a byte lookup or store through a pointer field at `gCurrentSceneVariable + 0x14`, load the scene-variable base and table pointer as separate locals, then use `table[arg1 * 0xE + arg0]`. The getter `func_08030F9C` and setter `func_08030F7C` both preserve the target's `LSLS #3; SUBS; LSLS #1; ADDS` index in R2 and the final `ADDS` into the table pointer.
- **Early-return branch layout remains source-sensitive**: `func_080020FC`'s direct null-check C was semantically exact but emitted `BEQ` with an extra `MOVS/B` block rather than the target's `BNE` fall-through and single zero-return block. Keep this as a near-miss seed; changing the condition/control-flow shape is required before retrying, not a reason to weaken the ROM gate.
- **Pointer-relative halfword initialization can lose the target's register/zero order**: `func_08035FEC`'s natural three-store C spelling omitted the target's explicit `MOVS #0`, used a different base register, and folded the final `+0x8C` address. Preserve the receipt as evidence and test a pinned base/zero ordering permutation if the family becomes worthwhile.
- **Pure-leaf signed absolute / clamp spellings**: `s32 result = (s16)value; if (result < 0) result = -result; return (s16)result;` preserves the target's sign-normalization shifts around a conditional `RSBS`; a direct lower/upper-bound `if`/`else if` clamp reproduces the compact `CMP`/conditional-move branches in `func_08008058`. These two helpers are now accepted real-C examples even though their target symbol metadata is locally truncated in isolated linked comparisons.
- **Clamp branch-layout shaping**: when the target branches with `BLS` to a later constant-return block and falls through to the arithmetic case, write the C condition as `if (temp <= 0x3F) return 0x7F;` followed by the arithmetic return. The semantically equivalent `if (temp > 0x3F) return computed; return 0x7F;` emits `BHI` to the constant path instead. `func_080F1FB4` is the exact real-C example.
- **Boolean return branch direction**: for a target `CMP; BNE; MOVS R0,#0; B; MOVS R0,#1` shape, write `if (value != 0) return 1; return 0;`. A source-level `if (value == 0) return 0; return 1;` is semantically identical but emits `BEQ` with the return constants reversed. `func_08016F60` records both near miss and exact spellings.
- **Byte-count epilogue width**: if a loop counter is already normalized in a register and the target ends with `MOV R0,R1`, use a `u32` return type even when the logical value is a byte. A `u8` return can add a final `LSLS/LSRS` sign/zero-normalization pair. `func_080F2C68` is the accepted example.
- **Empty output constraint for a required register copy**: when a pinned value must remain in R3 and then copy to R0 as `ADDS R0,R3,#0`, a plain `result = value + 0` can still coalesce the value into R0. An empty `asm("" : "+r"(value))` constraint keeps the compiler-visible R3 value live; it emits no instruction and lets the ordinary C assignment generate the target copy. `func_0801F698` is the accepted example. Do not put instruction text in the constraint.
- **Scene-state scaled-offset setters**: for small helpers that reload `gCurrentSceneVariable` between stores, pin the global-pointer register and each offset/result register, then spell the target's shifts as separate C statements (`offset = 0xE6; offset <<= 2`). This preserves sequences such as `LDR base; MOVS/LSLS offset; ADDS; store; LDR base; literal offset; ADDS; store`. The five Batch 200 siblings (`080B27B8`, `080C6898`, `080D2768`, `080D286C`, `080D28A4`) are exact examples.
- **Beatscript dual-store destination shaping**: when two large-offset stores use the same pinned scene base but the target writes the first computed address through `R2` and the second through `R1`, reassign the pinned base pointer for the second destination (`base = (u8 *)((u32)base + offset)`) instead of storing through a second temporary. `func_0800C9C0` uses this ordinary-C spelling to preserve both `ADDS R2,R1,R0` and `ADDS R1,R1,R0`.
- **Empty output constraint against mask folding**: after a known value has been ORed with a field and the target still needs separate `MOVS #N; RSBS` mask materialization, an empty `asm("" : "+r"(mask))` before `mask = -mask` can keep the compiler from folding the expression into a single `SUB`. `func_0800CA5C` is the accepted example; the constraint emits no instruction and is not volatile or instruction-bearing asm.
- **Absolute runtime-table symbol and operand order**: declare `D_0300XXXX` runtime addresses as extern symbols, initialize the pinned destination register from `&D_...`, and write `r1 = r0 + r1` when the target's `ADDS R1,R0,R1` operand order matters. `func_080F2894` matches this shape; add the canonical symbol assignments to `undefined_syms.ld` before applying.
- **Absolute runtime byte-table setters**: an `extern u8 D_030068F0[]` plus four sequential byte stores reproduces the `PUSH {R4,LR}` / absolute-literal / four-`STRB` / interwork-safe epilogue shape in `func_080F3C60`. Mirror the canonical `D_0300XXXX` assignment into `undefined_syms.ld` before applying a standalone TU.
- **Scene-data accumulator ordering**: declare the `gCurrentSceneVariable` pointer before the `gCurrentSceneData` pointer when the target loads the variable base first, then reads the scene halfword and adds it into the variable field. The ordered locals in `func_080DA0B0` preserve both literal-load order and the `LDRH`/`ADDS` register roles.
- **Graphics indexed-transfer accumulator**: for the `LSLS R0,#24; LDR R1,=gGraphicsBuffer; LSRS R0,#23` family, pin the offset to `R0`, the graphics base to `R1`, assign the base between shifts, use `offset = (u32)base + offset`, then add the source/destination byte offsets. This reproduces `func_08082934` without an instruction asm block.
- **Runtime table lookup accumulator**: when a `D_0300XXXX` symbol contains a runtime base pointer, declare it as a pointer, pin it to `R1`, pin the scaled index to `R0`, and use `offset += (u32)base` to preserve the target two-operand `ADD R0,R1`. Add the canonical symbol assignment before applying the standalone TU.
- **Runtime table two-store load order**: for a helper that stores two adjacent bytes through a pointer held in `D_0300XXXX`, bind the symbol address to `R4`, bind the first pointee to `R3`, shift the `R0` index afterward, and reload the pointee into `R1` between stores. `func_080F0E14` matches this shape; direct `D[offset]` indexing shifts before the first load or inserts `ADD R1,R4`.
- **Mask result orientation**: if the target loads the field into a separate register and performs `AND mask, value`, write the C as `mask &= value; mask |= replacement;` rather than `value &= mask`. Register-bound mask/result locals can preserve this orientation. Some adjacent scene-data setters remain blocked when agbcc reuses the prior `#1` literal and folds the desired `MOVS #2/#3; RSBS` into `SUB #3/#4`; keep those as near-miss evidence.
- **Runtime mask-materialization trap**: the same constant-folding issue applies to `D_030068E8` byte flags. After `arg1 &= 1` uses `R2 = 1`, ordinary C `mask = -5`/`-9` can become `SUB R2,#6/#10`; do not accept the semantically correct near miss when the target has `MOVS R2,#5/#9; RSBS`.
- gGraphicsBuffer small store pairs / clears
- **Adjacent gGraphicsBuffer halfword clears**: `gGraphicsBuffer.unk4C = 0; *(u16 *)((u8 *)&gGraphicsBuffer + 0x4E) = 0;` reproduces the `LDR; ADDS #0x4C; MOVS; STRH; ADDS #0x4E; STRH; BX LR` family. Keep the first field access and the raw byte-offset halfword store as separate statements; direct struct reshaping can change the base/add ordering.
- **Clear-then-call siblings**: append the already-prototyped `func_0800CDB0(1)` or `func_0800418C()` as a separate statement after that clear pair. The same ordinary-C body reproduced four additional standalone functions in Batch 176, including the target's interwork-safe `POP {R0}; BX R0` epilogue.
- `gGraphicsBuffer` 1-bit field writes like `gGraphicsBuffer.unk854_1 = arg0`
- **GraphicsTable pointer-advance loop**: forward-loop through `GraphicsTable` entries looking for `src == NULL` terminator, then tail-call. Use register-pinned pointer (`register char *r2 asm("r2")`) with goto labels to preserve `ADDS R2,#0xC` instruction order before the load/compare. The pattern is:
  ```c
  register char *r2 asm("r2") = arg0;
  goto start;
loop:
  r2 += 0xC;
start:
  if (*(void **)r2 != NULL)
      goto loop;
  func_0800247C(r2);
  ```
- **Eight-byte table-copy family**: for records whose word at offset zero terminates the table and whose byte fields at offsets four and five must be copied/cleared, use two register-pinned `u32 *` pointers, a goto-shaped check-first loop, and explicit byte-offset stores. The `func_08002FC0`/`func_08002FE8` bodies and their `func_08003028`/`func_08003040` scanners matched with this spelling; the `func_08003058` wrapper additionally keeps its incoming R1–R3 values in widened `u32` locals and calls the initializer through an old-style declaration so no caller-side byte truncation is introduced.
- `gCurrentSceneData` halfword add / shift / store helpers
- `sprite_id_delete(gSpriteHandler, *(u32*)(gCSV + offset))` siblings

## **NEVER decompile callers of already-converted C functions**
A 100% isolated compile match with `compile_and_view_asm` does **NOT** guarantee a final ROM match when the callee has already been converted to C. The isolated test compares your new C code against the **original asm callee**, but the linked ROM uses the **converted C callee** with potentially different register allocation for the call.

Example trap: `func_08002514` calls `func_080024D0`. Both were originally asm. `func_080024D0` was converted to C. Isolated testing of `func_08002514` used the original asm `func_080024D0`, producing a 100% match. But linking with the C `func_080024D0` changed the register allocation, causing ROM mismatch.

**Mitigation**: Use `decomp_siblings` with `strategy: callees` to check if any callees are already converted. If so, either:
1. Choose a different standalone function whose callees are all in asm
2. Or accept that the conversion requires manual verification beyond isolated testing

## Code-shaping rules that have proven important
- `#include "global.h"` in every decomp TU
- add `#include "types.h"` when touching g-symbols declared there
- add `#include "scenes.h"` when touching `gCurrentSceneData`
- use a local pointer variable when `((u32*)&gGlobal)[N]` would otherwise collapse into the wrong literal-pool expression
- `gCurrentSceneVariable` is a `struct BeatscriptLocalData *`, so raw `gCurrentSceneVariable + N` scales by `sizeof(struct BeatscriptLocalData)`; use `(u8 *)gCurrentSceneVariable + off` for byte offsets, or an intentional typed index like `((u32 *)gCurrentSceneVariable)[N]`
- **gCurrentSceneData shift-offset loads**: `ptr = (u8 *)gCurrentSceneData; return ptr[N << shift];` reproduces the LDR+MOVS+LSLS+ADDS+LDR[BH] sequence. The shift constant (e.g. `0xBA << 1` = 0x174) encodes as `MOVS R1, #0xBA; LSLS R1, R1, #1`. Use `u8 *` cast for byte loads, `*(u16 *)(ptr + offset)` for halfword loads.
- **Load-base-first trick**: When the original has LDR before LSLS (loading a global base, then shifting the index), assign the base to a local first: `u32 *base = D_0300400C; ptr = (u16 *)((u8 *)base + (arg0 << 2));`. Without the local, agbcc may emit LSLS before LDR.
- **Load-base-first for literal-pool offset stores**: When storing to a large offset from a global struct (e.g., `gBeatscriptScene.unk1C58` at offset 0x1C58), declare `u8 *base = (u8 *)&gSymbol;` first, then compute the destination: `u16 *dest = (u16 *)(base + 0x1C58);`. This matches the original's instruction sequence of `LDR R2,=gSymbol; LDR R3,=offset; ADDS R0,R2,R3; STRH R1,[R0]` rather than having the compiler fold the offset into a single literal-pool address.
- **Pointer-advance for zero-init/struct-init**: When the original does `ADDS R0, #0xC` then stores at [R0], [R0+4], [R0+8], reassign the pointer: `a0 = a0 + 3; a0[1]=0; a0[0]=0; a0[2]=0;`. Using absolute offsets `a0[3]=0; a0[4]=0; a0[5]=0;` generates offset-from-original instead of pointer advance.
- For RSBS-mask-clear pattern on a global struct: declare `u8 *p = (u8*)&gSymbol;` then use `p[offset]` to keep the global base in one register and the offset in the instruction; do NOT use `((u8*)&gSymbol)[offset]` directly (agbcc combines into a literal+relocation with zero offset).
- `s16-indexed byte-store` pattern: use `a0 = (u32)(s16)a0; a1 += 0x80; a1 += a0; *a1 = value;` to match LSLS/ASRS, ADDS R1 #imm, ADDS R1 R0 instruction order. Using `a1[(s16)a0 + 0x80] = value` or `*(a1 + 0x80 + (s16)a0) = value` generates ADD-to-R0 not ADD-to-R1.
- for post-BL stores, declare locals before statements and assign after the BL if needed to preserve C89 compliance
- when the original reuses the same base load for multiple stores, mirror that with a local pointer for the first stores and only fall back to the raw global on the final store if needed
- **GBA BIOS SVC lowering**: `func_080EE61C` is real C through the target-specific `__builtin_swi_div()` added by `tools/agbcc-swi.patch`. The builtin models the fixed BIOS ABI (incoming `r0/r1`, quotient in `r0`) and expands through an agbcc Thumb backend `define_insn`, producing exact `SVC #6; BX LR` bytes without source-level instruction asm. Ordinary C `/` remains unsuitable because it calls `__divsi3`.
- **Shifted scene-data accumulator siblings**: for wrappers that load `*(u16 *)((u8 *)gCurrentSceneData + 0x16)`, shift it with `>> 3`, then add it to a `u32` field in the argument object, the direct pointer-arithmetic C form reproduces the target `LDRH; LSRS; LDR; ADDS; STR` sequence. Batch 167 matched four offset variants at `0x3C`, `0x14`, `8`, and `0x28`; keep the source and destination expressions in the same statement shape.
- **Large global-offset stores**: when agbcc folds a large global-relative offset into one relocation but the target has separate global and offset literals, pin a `u8 *` base in `r1`, pin a `u32` offset in `r2`, and place an empty `cc`-neutral barrier before `*(u16 *)(base + offset)`. Batch 168 reproduced `LDR base; LDR offset; ADDS; STRH` for offsets `0x1C32` and `0x1C30` without non-empty inline asm.
- **Indexed scene-variable halfword stores**: load a `u8 *` scene-variable base first, compute `index << 1` separately, then add the shifted `0x83 << 2` base offset before the final `u16` store. This source order reproduced `func_080AAA40`'s `LDR; LSLS; MOVS; LSLS; ADDS; ADDS; STRH` sequence.
- **Scene-variable halfword accumulator**: pin the global base to `r1`, sign-extend the incoming value in `r0`, load the existing halfword into a pinned `r2` temporary, then write `r0 += r2` back. A compound assignment puts the destination in `r2` and reverses the target `ADDS R0,R2` operand order.
- **Reloaded scene-variable byte pair**: pin the address of `gCurrentSceneVariable` to `r2`, reload the pointed-to scene object for each store, and assign each byte constant after its address add. This preserves the target's `LDR [R2]` reload and avoids hoisting `MOVS` before `ADDS`.
- **Delayed zero / call-then-clear shaping**: initialize a pinned zero only after the target address add when the target places `MOVS #0` there; keep a preceding helper call as its own C statement before the graphics-buffer store. Batch 179 matched both the literal-pool zero placement (`func_080195E4`) and the call-then-clear wrapper (`func_080DCD54`).
- **Non-void wrapper epilogue**: when a semantically void wrapper ends in `POP {R1}; BX R1`, declare the C function with a non-void return type and omit a synthetic return value. `func_0809C47C` uses this to preserve the interwork-safe epilogue without inline instruction asm.
- **Conditional-return placement trap**: the natural `if (arg0) return arg0->field; return 0;` spelling for `func_080020FC` remains a near miss because agbcc places the zero return before the branch target. Keep the rejected candidate recorded and try a different control-flow shape rather than accepting a semantically equivalent but nonmatching body.
- For hardware register writes: use `*(volatile u16 *)0x4XXXXXXX = val;` to match literal-pool address generation and store
- For IWRAM absolute-address byte store with non-zero offset: use a local struct typedef with fields laid out to put the target byte at the correct offset, then cast the address: `(*(StructType *)0x0300XXXX).field = val;`. Using `((u8 *)0x0300XXXX)[offset]` causes agbcc to fold the offset into the literal pool address, producing a different object. Also use `s32` parameter type instead of `u8` to avoid the compiler inserting `LSLS R0, #24; LSR R0, #24` u8 masking.
- **ROM absolute-address halfword load with non-zero offset**: when the target literal is an absolute ROM address and the instruction uses a separate `[base, #N]` offset, use a local pointer such as `u16 *ptr = (u16 *)0x086F277C;` followed by an empty `asm volatile("" : "+r"(ptr))` barrier before `ptr[N]`. Direct indexing lets agbcc fold `N` into the literal address. Example: `func_0803FED0` preserves `.word 0x086F277C` plus `LDRH [R0, #2]`.
- For sequential byte writes: use `u8 *p = *a0; *p = byte0; p++; *p = byte1; p++; *a0 = p;` — pointer increment `p++` pattern generates `ADDS R2, #1` matching the original asm, while array indexing `p[0]`/`p[1]` uses different offset forms
- D_ symbols used in C files need entries in both `include/undefined_syms.inc` (for asm preprocessing) AND `undefined_syms.ld` (for linker resolution from C objects). When converting from asm to C, any D_ symbol referenced in the C code must be added to `undefined_syms.ld` or the linker will fail / produce a different ROM.

## Code-shaping rules that have proven important

### agbcc u8-increment high-byte idiom (batch 245)
- When a target increments a byte-width counter with the 5-instruction sequence `lsl rT,rN,#24 / mov rM,#0x80 / lsl rM,#17 (=1<<24) / add rT,rM / lsr rN,rT,#24`, the only ordinary-C spelling that reproduces it is a single expression on a **u32** variable: `index = ((index << 24) + 0x1000000) >> 24;`.
- Compound spellings (`index++`, `index = index + 1`, `(u8)(index + 1)`, even on a declared-`u8` variable) are canonicalized by agbcc into the fused add-first truncate form (`add r0,rN,#1; lsl #24; lsr #24`). In-place compound shifts (`index <<= 24; index += ...; index >>= 24`) keep the right sequence but swap the temp/destination register roles. Use the single-expression form.

### Relocation-vs-baked-address pool equivalence (batches 251/252)
- Original-ROM-derived asm targets bake absolute addresses into pointer-literal pools, while agbcc C candidates emit relocations (placeholder zeros). Compare three things instead of raw bytes: instruction bytes, pool slot offsets/order, and the relocation symbol+offset list. If every reloc maps to the same offset/symbol as a baked word whose address matches `undefined_syms.ld`, the sections are link-equivalent even though bytes differ.
- Named struct/bitfield members already present in `include/types.h` (e.g. `currentThread:3`, `localVariables[]`, `threads[].sprites`) frequently reproduce agbcc codegen exactly on the first spelling — check the header for named members before hand-writing masks or offsets.

### Bitfield-insert recognition and return-type deduction from epilogues (batch 251)
- Overlapping byte-then-word read-modify-writes into one stack word (byte mask at bits 0..1 via `ldrb/strb`, then word mask like `(x & 0xFFFE0003) | ((v & 0x7FFF) << 2)`) are almost certainly C bitfield stores (`u32 lo : 2; u32 mid : 15; ...`). Declare the bitfields instead of hand-writing masks: agbcc then generates the exact mask/load/store sequence AND the same operand register allocation (value masked in place in its arg register, container loaded into another). Hand-spelled mask expressions produce equivalent-but-permuted registers that never converge.
- `pop {rX}; bx rX` epilogue register choice is a return-type oracle: `pop {r0}; bx r0` for void functions, `pop {r1}; bx r1` when r0 carries a live return value. If your void candidate pops r0 but the target pops r1, make the function return the value of its final call (`return start_new_task(...);`) instead of discarding it.
- Reading an uninitialized local's low bits before the first store (e.g. bitfield store into a fresh stack struct) is expected and matches the original compiler output; do not zero-initialize.

### Host-TU declaration conflicts for included stubs (batch 250)
- An included_stub candidate's extern declarations live inside the host TU, which may already declare or define the same functions with different types via other included decomp files or its own body. Before applying, grep the host source and every `../decomp/*.c` it includes for each extern you add; match the existing spelling exactly even if your preferred type is more accurate (e.g. use `extern u16 get_current_mem_id(void);` when siblings do, and keep the `(u16)` cast at the call site — codegen is identical).
- Symptom if missed: clean build fails with "conflicting types for X" pointing at two include-line numbers inside the same TU; the transactional apply rolls back automatically and the receipt's `full_verify.output_tail` names both files.
- When assembling an included-stub target object manually for boundary audits, include `gba.inc` (not just `asm_macros.inc`) so pool words like `VRAMBase + 0x8000` get real values instead of a zero symbol plus relocation; otherwise SHA comparisons mislead.

### Included-stub manual .text boundary audit (batch 249)
- When the isolate receipt reports the known included-stub spurious near miss, prove exactness with a complete-section comparison instead: compile the candidate standalone with agbcc (strip the `__INCLUDE_LEVEL__` guard lines first — the guard suppresses everything when compiled with level 0), strip `.size/.ident/.global`, assemble, then `objcopy -O binary --only-section=.text` both candidate and target objects and compare SHA-256. Matching full-section hashes including literal pools are stronger than instruction-only diffs (batch 220/221 precedent).
- Eight-argument calls lower in agbcc as: arg1 into r0, stack args 5..8 stored to [SP]/[SP+4]/[SP+8]/[SP+0xC] in forward order, then register args r1/r2/r3 materialized last. Natural C argument order reproduces this; declare unconverted callees as `extern void f(u32 *, u32, u32, u32, u32, u32, u32, u32)` since all word-sized arguments pass identically.
- A store through a byte pointer into scene data forces the compiler to reload `gCurrentSceneData` for later uses (possible aliasing), which is exactly what the target's second `ldr r1,[r3]` shows — no barriers needed.

### Pin-free rewrite of legacy pinned files (batch 248)
- When a batch must edit a legacy file that still carries compiler register pins, the guard requires converting the whole file to strict C first. Reuse the converted asm under `asm/converted/` as the comparison target: extract the inner string (unescape `\n`, drop line-end backslashes), prepend `.include "asm_macros.inc"`, assemble with `arm-none-eabi-as -march=armv4t -I include` into `build/asm/asm_<addr>.s.o`, then `compile_and_view_asm` can verify the rewrite exactly.
- Reload semantics do the work: global reads around calls (`D_03006518.unk2` before each of two calls) are re-emitted per call because calls clobber globals, matching the target's paired `ldrb r0,[r4,#2]` without pins; the base address pseudo stays live across calls and lands in r4 on its own.
- Byte-clear statement order controls register homes: `val = *flags; mask = 2; mask = -mask; mask = val & mask; *flags = mask;` yields `ldrb r2` / `movs r0,#2; neg r0; ands r0,r2` / `strb r0` — the load precedes the mask materialization, so the byte temp takes r2 and the mask takes r0 with AND destination = mask register.

### Switch dispatch and register homes (batch 248)
- agbcc lowers a small dense unsigned switch as binary-search comparisons against the middle case (`cmp #1; beq case1; cmp #1; blo case0; cmp #2; beq case2`). A `u32` parameter reproduces this exactly; a `u8` parameter adds an `lsl/lsr #0x18` widening prologue and flips dispatch to signed `bgt`, so prefer `u32` parameters when the target shows no widening.
- Pure-expression statements (`*(u8 *)((u8 *)global + off) |= mask;`) allocate the address temp to r0, the pointer to r1, the byte to r0, and the mask to r2. The same logic through a named pointer local consistently lands the pointer in r2 with the mask in r1 — use expression form when the target shows that home pattern.
- When a host TU's accepted sibling files carry stale conflicting prototypes for the candidate (e.g. `extern void f(u8)` vs the true `u32`), fix the sibling extern lines as part of the transaction. Call sites passing zero-extended byte values are codegen-neutral under the widened prototype; the byte-identical ROM gate is the proof. Run apply with `WARIOWARE_ALLOW_DIRTY=1` only when the dirty paths are exactly those coordinated edits.

### Included-stub host-TU prototype consistency (batch 246)
- When adding a candidate to a host TU that already has accepted decomp files, reuse those files' existing helper extern prototypes verbatim (e.g. `sprite_remove_z_link(void *, s16)` / `sprite_update_z_link(void *, s16)` in `src/lib_sprite.c`). Declaring the same helper with a differently-typed first parameter (`struct SpriteHandler *`) inside the same TU is a hard conflicting-types build error; the cycle rolls back cleanly but the chunk loses a full-build cycle. Grep the host TU's `src/decomp/` files for the helper name before writing externs.
- High-register allocation for narrowed stack/argument locals follows declaration order: declaring/initializing `x, y, z` in that order placed x in r7, y in SB, z in R8 for `sprite_set_x_y_z`, matching the target even though the target loads the stack argument first.

### Strict-audit layout traps (batch 247)
- Overlay gap arrays (`u8 pad[0x24];`) inside a named overlay are rejected as opaque byte-pointer layouts even when the overlay also has named fields. Re-anchor the overlay at the nearest existing named field (e.g. `&handler->unk20`) and model only the bytes you actually touch.
- Chained subscripts through a raw scalar alias (`oam[0] = oam[1] = ... = v;`) count one numeric offset per index in the layout gate; the bounded threshold is two. Model fixed-stride block fills with a named struct of explicit members and a chained member assignment instead — identical codegen (descending word stores + stride advance), zero raw-offset evidence.
- A raw-alias pointer walk with no numeric arithmetic (`*word++ = v;`) is audit-free; introduce the u32 cursor only at the point where word granularity starts.

### agbcc scheduler/register shaping (batch 247)
- Constant materialization statements (`mask = -0x10;`) are scheduled at their statement position; to get a mask built AFTER a load, make the load a separate preceding statement and combine in a later one (`t = *flags; mask = -0x10; mask = mask & t;`).
- The commutative-AND destination follows the source operand order: `mask = mask & bound` emits `and mask_reg, byte_reg`; `bound &= mask` emits the reverse.
- Reusing a live short-lived local as a temp can block agbcc's "sub from zeroed register" constant trick; fresh locals may trigger it.
- LICM hoists `mov/lsl` constant materializations out of loops whenever a high register is free at regalloc time (see `sprite_clone` near miss). No pure-C spelling found that prevents it under low register pressure; treat such functions as near-miss evidence unless a pressure-increasing shape exists.

## Known traps
### Instruction ordering / code generation traps
- `a0 = (u32)(s16)a0` **before** the pointer constant-add forces the sign-extension instruction first (LSLS/ASRS before ADDS R1, #0x80). Writing `a1 += 0x80; a1 += (s16)a0` reverses the order even though it reads naturally in C.
- For global byte access via RSBS-mask-clear, use a local pointer `u8 *p = (u8*)&gSymbol` and then `p[offset]` to get `[R2, #offset]` addressing. Writing `((u8*)&gSymbol)[offset]` directly may produce a combined literal with the offset baked in, giving `[R2, #0]` instead.
- gGraphicsBuffer indexed halfword stores like `*(u16*)((u8*)&gGraphicsBuffer + 0x54 + (u16)a0 * 2)` can produce LDR-before-LSLS or LSLS-before-LDR depending on C form; the exact interleaved original asm pattern is hard to reproduce — leave as asm if the instruction ORDER differs.

### Header / include traps
- `gSpriteHandler` is declared in `src/lib_sprite.h` — include as `"src/lib_sprite.h"` (relative to repo root), not `"lib_sprite.h"`
- Never add a bare `extern void sprite_id_delete(u32, u32)` — it conflicts with the real `struct SpriteHandler *` signature; let lib_sprite.h provide it
- `gCurrentSceneVariable` is in `types.h`; `gGraphicsBuffer` is in `graphics.h`
- **Don't duplicate extern declarations** that already exist via transitively-included headers. E.g. `memory.h` includes `gameplay.h` which declares `func_08003FB8` and `func_08007EAC` — so `main_menu.c` already has them via `#include "src/memory.h"`. Adding your own extern in the decomp file causes conflicting types.
- **Included-stub extern type consistency**: When converting an included_stub function that uses symbols already declared in other included_stub files in the same host TU, the extern declarations must match exactly. E.g., `func_0800C7A4` was declared as `extern void func_0800C7A4(s32)` in asm_080117fc.c — a new file declaring it as `extern void func_0800C7A4(u32)` causes a compilation error in the host TU.
- **Same-TU callee prototype can be shaped if the callee remains byte-identical**: sometimes a caller needs a signed formal type to emit `LDRSH` for an argument, while the already-converted callee used `u16` to force zero-extension internally. It can be safe to change the callee prototype to `s16` and explicitly cast inside the callee (`stackArg = (u16)arg1`) so the callee still emits the original `LSLS; LSRS` but the new caller emits `LDRSH`. Example: `func_0800C0BC` needed `func_0800C080(s16, s16, ...)`; `func_0800C080` stayed byte-identical by assigning `(u16)arg1` to its stack struct.
- **Old-style same-TU callee declaration to avoid caller-side re-truncation**: when the already-converted callee's visible prototype would force unwanted `LDRH` or extra sign-extension in a new caller, an old-style declaration (`extern void *callee();`) can keep the call ABI unprototyped so agbcc passes already-shaped `s32` register values unchanged. This is safer than changing a shared callee prototype when only the new caller needs the spelling. Example: `func_0800C15C` calls already-converted `func_0800C110` after loading two generated coordinates with `LDRSH`; using `extern void *func_0800C110();` avoided re-truncating those values while preserving the callee's body.
- **Included-stub task-wrapper return propagation**: `func_0800C4E0` needed the fifth stack argument truncated into a named `u32` local before the coordinate helper call, and its task-helper result is returned as a typed `void *`. That ordinary-C spelling preserved the target's early `LSLS/LSRS` order and `POP {R1}` epilogue without a no-return non-void function, asm, barrier, or register pin. This is a useful host-TU wrapper shape when the call result is otherwise ignored by callers.
- **Included-stub helper ABI order / typed call target**: When a wrapper calls an already-converted same-TU helper, preserve the target's register/stack order at the call boundary with a unique typed function-pointer typedef rather than compiler-only tricks or an offset-based pointer blob. For `func_0800C218`, the exact order is `(arg0, x, y, arg1, arg2, arg3, arg4)`; an all-`s16` local call type plus named `u32` truncation temporaries reproduces it in ordinary C, with the helper's real `void *` return. Literal-pool-only isolated near misses such as `func_0800DE84` remain unaccepted until full-context proof; instruction similarity alone is not a byte-match waiver.
- **Included-stub direct existing-struct fields / call-before-load order**: For a same-TU main-menu wrapper whose record fields are already modeled, use the existing typed `gMainMenu` fields directly instead of introducing a local byte overlay. If a preceding call determines whether the fields are read, keep the field access inside that branch; hoisting a base assignment before the call can add an unnecessary callee-save register and change the prologue. A candidate-origin relocation-only isolated gap still requires the guarded full host-TU/ROM proof; it is never an instruction or metadata waiver.
- **Implicit-register-argument trap**: Some functions like `func_08015A88` take an argument in R0 implicitly (reading R5=R0 in their prologue), but existing decomp files declare them as `void func(void)`. If you need to pass R0 explicitly for a new conversion, changing the extern to `void func(u32)` breaks existing callers that pass no argument. The compiler generates `MOVS R0, #0; BL func` instead of just `BL func`, causing ROM mismatch. **Mitigation**: Leave such functions for later when all callers can be updated simultaneously; do not patch the call with a naked asm wrapper.
- **Forward declarations needed** when an included_stub function is used before its include point in the host C file. Add `extern void func_XXXX(void);` before the first use.
- **s16 callee-signature sign-extension trap**: When a function passes a sign-extended s16 value to a callee declared as `s32 sprite_is_invalid(void*, s16)`, the compiler adds extra `LSLS R1, #16; ASRS R1, #16` before the BL because it re-sign-extends the s16 argument per the callee's formal parameter type. The original asm doesn't have this — the value is already sign-extended in a register. **Mitigation**: First try declaring the callee with a matching s32 parameter type in your decomp file. If that causes conflicts with other decomp files in the same TU, try a narrow `asm volatile("bl callee")` call-site shim, but do not wrap the whole function in naked asm. Example legacy files: `sprite_set_x`, `sprite_set_y` should be revisited.
- **CMP#1/BGE vs CMP#0/BGT optimization trap**: The compiler (agbcc) transforms a direct `val >= 1` into `val > 0`, which generates `CMP R0, #0; BGT/BLE` instead of `CMP R0, #1; BGE/BLT`. For a byte-valued switch, use the resolved range-dispatch shaping documented below instead of reintroducing non-empty asm; `func_0800BEC0` uses cases 1–3, case 4, and a redundant unreachable negative case sharing the default target.
- **CMP#1/BLO vs CMP#0/BEQ optimization trap**: The compiler transforms `val < 1u` (unsigned) into `val == 0u`, generating `CMP R0, #0; BEQ/BNE` instead of `CMP R0, #1; BLO/BHS`. Same root cause as CMP#1/BGE — comparing against 0 is cheaper. When the original asm uses `CMP R0, #1; BLO` (checking if arg is 0 in a switch-like pattern), pure C cannot reproduce this exact sequence. **Mitigation**: Mark the candidate blocked. Example blocked function: `func_08011864`.
- **Register allocation mismatch with 5th stack arg + u16 truncation**: Functions that take a 5th stack argument AND truncate one of the register arguments to u16 (LSLS/LSRS) are extremely difficult to match with pure C. The compiler may swap which register gets the truncation (R0 vs R1) and which callee-save register holds the 5th arg vs the zero constant (R5 vs R6). Additionally, the compiler may place the stack buffer at different offsets (sp[0] vs sp[1]) causing all subsequent store offsets to shift. **Mitigation**: First try local stack structs/arrays, non-void return shaping, and register pinning to control which registers hold which values; `func_0800C110` proved this family can match in real C. If it still misses, keep shaping or choose another candidate — do not add naked asm. Example legacy file: `func_0800C080` should be revisited.
- **__divsi3 argument reorder trap (resolved for `func_08001C74`)**: When computing `__divsi3(0x10000, angle)`, an ordinary C call through the compiler's helper declaration now preserves the target setup; the former non-empty `bl __divsi3` shim is no longer needed.
- **MOV R4,R2 collapse into LSL R4,R2,#0x10 (resolved for `func_08001C74`)**: An ordinary `r4 = r2` followed by an empty `asm volatile("" : "+r"(r4))` barrier preserves the target two-instruction copy/shift sequence without embedding an instruction shim.
- **agbcc won't push R7 callee-save register**: When a function uses R7 as a loop variable or general-purpose register, the compiler may not push/pop R7 even though the original asm does. The compiler only pushes callee-save registers that it actually clobbers across function calls, and since R7 is callee-saved by convention, the compiler assumes BL calls preserve it. If R7 is only used between BL calls, the compiler doesn't save it. **Mitigation**: No reliable pure-C workaround found. `asm volatile` barriers and clobbers don't force the push. Blocked example: `func_08012BB8`.
- **ORRS value-propagation into LSLS collapse**: After a register is assigned a small constant (e.g., `R2 = 4` from `MOVS R2, #4; ORRS R0, R2`), if the same register is later reassigned as `MOVS R2, #0x80; LSLS R2, #1`, the compiler may optimize this to `ADD R2, #0xFC` (adding 0xFC to the existing value 4 to get 0x100). This is semantically identical but byte-different. **Mitigation**: Insert `asm volatile("" : "+r"(r2))` barrier after the ORRS to break the value-propagation. Example: `func_08013EC0`.
- **s16/s8 callee-signature register-reuse in LDRSH**: When calling a function like `sprite_set_base_palette(struct SpriteHandler*, s16, s8)` where R2 is used for both the LDRSH offset and the third argument, the compiler may swap R2 and R3 — putting the LDRSH offset in R3 and the constant in R2. The pure-C workaround is a unique local function-pointer typedef with widened `s32`/`u32` ABI parameters, which preserves the already-shaped registers without a non-empty BL shim. This is proven for `func_08014810`, `func_08014E38`, `func_08014E88`, `func_0800BC10`, and `func_0800BC50`.
- **Skipped middle-parameter ABI shape**: If the original consumes a value from `R2` while `R1` is unused, model the gap with an unused middle C parameter. A two-parameter spelling makes the value arrive in `R1` and changes the sign-extension sequence. Example: `func_080F2F68` uses `void func(u32 *arg0, s32 unused, s16 arg2)` to preserve `LSLS R1,R2,#16; ASRS R1,#16` before the call.
- **asm volatile BL clobber scope (historical)**: Earlier wrappers used BL shims and had to model callee-save clobbers carefully. The accepted maintenance pass replaced the affected `func_08013388` family with ordinary C calls through ABI-shaped typedefs, so new conversions should use that pattern first.
- **schedule_function_call u16 first-arg re-truncation (resolved for `func_0800A0C4`)**: A local function-pointer typedef with a `u32` first argument avoids re-truncating an already-shaped memory ID while retaining the ordinary C call and the existing empty barrier used for literal-load ordering.
- **Compiler hoisting literal pool loads before BL**: After a BL call, the compiler may hoist subsequent `LDR Rn, [PC, #offset]` (literal pool loads) above the BL if there are no data dependencies. The original asm loads them after the BL. **Mitigation**: Insert `asm volatile("" : "+r"(r0))` after the return value truncation to prevent the compiler from reordering. Example: `func_0800A0C4`.
- **mem_heap_dealloc(u32) vs mem_heap_dealloc(void*) type conflict**: `src/memory_heap.h` declares `void mem_heap_dealloc(void *)` but existing decomp files in main_menu.c use `void mem_heap_dealloc(u32)`. Including `memory_heap.h` in a new decomp file included in main_menu.c causes a conflicting types error. **Mitigation**: Don't include `memory_heap.h`; declare `extern void mem_heap_dealloc(u32)` to match existing same-TU extern declarations. Example: `func_0801522C`.
- **MOVS+RSBS constant-folding into SUB**: When code has `r0 = 0xF; r0 = -r0;` and r0 previously held a different value (e.g., 7), the compiler folds this into `SUB R0, #0x16` (computing `7 + (-0xF) = 7 - 0x16`). This produces `SUB` instead of `MOVS; RSBS`. No pure C workaround found — even asm volatile barriers and separate register variables don't prevent the fold. The compiler's constant propagation is too aggressive. **However**, if R0 previously held an unknown value (e.g., address of a global loaded from literal pool), the compiler CANNOT fold, and `MOVS R0, #N; RSBS R0, R0, #0` is emitted correctly. Blocked example: `scene_set_current_thread` (R0=7 was known). Working example: `func_08012658` (R0=gCurrentSceneData address was unknown).
- **Symbol address vs numeric constant in literal pool**: When a function stores a ROM address constant (e.g., `0x083A8588`) via `LDR R0, [PC, #offset]; STR R0, [R1]`, using `r0 = 0x083A8588` produces `.word 0x83a8588` in the literal pool (numeric constant). The original uses `.word D_083A8588` (symbol relocation). These have the same value but different object file entries, causing objdiff mismatches. **Mitigation**: Declare `extern u8 D_083A8588;` and use `r0 = (u32)&D_083A8588` instead of the numeric constant. Example: `func_0800DE24`.
- **CMP#1;BLO vs CMP#0;BEQ codegen**: The original compiler emits `CMP R0, #1; BLO` for `if (x < 1)` in switch-case lowering. Modern agbcc optimizes this to `CMP R0, #0; BEQ` since `x < 1` for unsigned is `x == 0`. These are semantically identical but byte-different. No pure C workaround found — the compiler always folds `CMP #1; BLO` into `CMP #0; BEQ`. Blocked example: `func_08011864`.
- **IP/R12 register not usable in pure C**: When the original asm uses `MOV IP, R0` to save a value in the IP register (R12), pure C cannot replicate this. The compiler uses R6 or another callee-save register instead, changing the PUSH/POP frame and register allocation. Blocked example: `func_08014740`.
- **Function pointer literal `.hword` vs `.word` in literal pool**: When a function stores a function pointer like `LDR R1, =func_08012D3C + 1` in the literal pool, the C compiler generates `LDR R1, [PC, #offset]` correctly but the assembler emits `.hword func_08012D3C+0x1` instead of `.word func_08012D3C+0x1`. This 2-byte vs 4-byte difference shifts all subsequent literal pool entries and causes ROM mismatch. The assembler appears to pack the function pointer address into a 16-bit `.hword` relocation. **No pure C workaround found** — even declaring as `extern void func()` and using `(u32)func + 1` produces `.hword`. The isolated compile shows 0 instruction differences, but the full ROM build fails to match. Blocked examples: `func_08012D7C`, `func_0800A2D8`.
- **Same-TU callee-risk false positive for asm stubs**: When the candidate calls a function that is still an asm stub (not yet converted to C), the callee-risk warning is a false positive. The compiler treats `BL func_xxx` as an opaque call when the callee is an asm include — it doesn't see the callee's implementation or signature. Working example: `func_08013E64` calls `func_08011698` (still asm stub) with no issues.
- **Stale build deps after failed apply_conversion**: After `apply_conversion` fails and auto-restores, stale `.d` dependency files in `build/` may reference the now-deleted decomp C file, causing `make` to fail with "No rule to make target". **Mitigation**: `rm build/src/<tu>.d build/src/decomp/asm_*.d` before retrying the build.
- **TU-wide literal pool layout mismatch**: When converting an included_stub function in a large TU (like main_menu.c with 70+ asm stubs), the isolated compile shows 0 instruction differences and a perfect match, but the full ROM build fails to match. The compiler merges and reorders literal pool entries across the entire TU when compiling C instead of including asm stubs, changing the binary layout of the function's literal pool section. This causes a ROM-level byte mismatch even though the function's instructions are byte-identical. **No known workaround** — the literal pool layout depends on the compiler's TU-wide optimization decisions. This affects functions with multiple literal pool entries (data symbols, numeric constants) that the compiler may deduplicate or reorder. Blocked examples: `func_080119EC` (130 instrs all match, 6+ literal pool entries), `func_0801216C` (32 instrs all match), `func_0800A2D8` (38 instrs all match). Note: functions with FEWER literal pool entries or in SMALLER TUs may still work (e.g., `func_08013E64` worked with only 1 literal pool entry).
- **Copy propagation merges sign-extension + register copy**: When the original asm does `LSLS R5, R5, #16; ASRS R5, R5, #16` (s16 sign-extend into R5) followed later by `ADDS R3, R5, #0` (copy R5 to R3), the compiler merges these into a single `ASRS R3, R5, #16` (sign-extend directly into R3, skipping the intermediate R5 result). This also causes instruction reordering: the compiler interleaves R4's `LSLS; ASRS` pair between R5's `LSLS` and the deferred `ASRS R3, R5, #16`. **No pure C workaround found** — `asm volatile("" : "+r"(r5))` barriers, `asm volatile("mov %0, %0" : "+r"(r5))`, volatile writes, and even inline asm with `lsls; asrs` output constraints are all optimized away by agbcc's copy propagation pass. The compiler recognizes the mathematical equivalence and folds regardless of barriers. Blocked example: `func_0800DE84` (all 43 instructions match in isolated compile, but full TU compilation produces 49 different bytes due to copy propagation). This is distinct from the TU-wide literal pool issue — the function's instructions themselves change.
- **agbcc conditional-return default-MOV-before-B trap**: For non-void functions with early conditional returns, agbcc always emits a `MOV R0, #default_val` before the unconditional `B done`. The original instead places the default value MOV **after** the B (in the else-branch slot). So original: `CMP; BEQ; LDR; B; MOVS; POP; BX` vs compiled: `CMP; BEQ; LDR; MOV; B; POP; BX`. Both have the same instruction count and byte range, but the layout diverges at the 6th instruction. The diff shows `(end)` for target indices 5-7 because the compiled stream occupies positions the original doesn't. **No pure C workaround** — various C spellings (if/else, ternary, switch, goto, intermediate variable) all produce the same compiled pattern. Blocked example: `func_080020FC` (null-pointer-guarded pointer-dereference wrapper). Consider marking the candidate blocked or leaving the original asm file untouched.

### `_call_via_r1` indirect call trap

When the original asm loads a function pointer into R1 via `LDR R1, =func_XXXX` and then calls `BL _call_via_r1`, pure C generates a direct `BL func_XXXX` instead. The `_call_via_r1` pattern uses R1 for the callee address and R0 for the argument, producing `LDR R1, [PC, #offset]; MOV R0, R2; BL _call_via_r1`. A direct C call produces just `MOV R0, R4; BL func_XXXX` which is different object code.

**Current status**: This is a legacy hard case — no pure C spelling has been found for the indirect R1 call shape. Existing naked files with this pattern may remain quarantined until a real-C workaround is found, but this pattern does not justify adding new naked asm progress. Example legacy file: `func_080EE830`.

### s32 casts for ASR in signed multiply-shift
When computing `s32_result = (s16_val1 * s16_val2) >> 8`, the compiler may generate LSR instead of ASR if the multiply operands are in u32 register variables. Use explicit s32 casts to force ASR:
```c
register u32 r0 asm("r0");
r0 = (u32)*(s16 *)(r3 + r5);
r0 = (u32)((s32)r2 * (s32)r0);  // MUL with signed operands
r0 = (u32)((s32)r0 >> 8);        // generates ASR #8, not LSR #8
```
Without the s32 casts, the compiler treats the MUL result as unsigned and generates LSR. Used in func_08001BA4 and func_08001C08 (rotation matrix builders).

### Register / return-shape traps
- `POP {R1}; BX R1` epilogue is generated by agbcc for **non-void** functions with `-mthumb-interwork` (the return value is in R0, so it uses R1 for the pop). **Void** functions with `-mthumb-interwork` generate `POP {R0}; BX R0`. Without `-mthumb-interwork`, both generate `POP {PC}`.
- BL + STRH patterns may keep the wrong register live and produce `POP {R0}; BX R0` instead of `POP {R1}; BX R1` — check if the function should be non-void
- `s8`/`s16` return types can force extra sign-extension in the epilogue (`LSLS`/`ASRS`) even when the original just moves the register through. If that happens, try a wider intermediate and confirm whether the sign-extension is truly required or just a compiler artifact.
- `start_new_task` wrappers with on-stack task-arg structs often need to return the call result (`return start_new_task(...)`) to preserve the non-void epilogue (`POP {R1}; BX R1`) while still keeping the stack block in place.
- semantically identical C can still miss due to register allocation differences
- Zero-init store order depends on C source statement order: `a0[1]=0; a0[0]=0; a0[2]=0;` generates `STR [R0,#4]; STR [R0]; STR [R0,#8]` (matching the original's non-sequential pattern), while `a0[0]=0; a0[1]=0; a0[2]=0;` generates sequential stores
- Included stubs in the same TU don't need extern declarations for functions already defined in the host C file
- **Instruction ORDER is critical for byte-identical matching**, not just instruction choice. `.syntax divided` makes `mov`/`neg`/`and` encode identically to `movs`/`rsbs`/`ands`, but the compiler may reorder instructions differently from the original. For example, `MOVS R0,#3; RSBS R0,R0,#0; ANDS R0,R1` and `LDRB R1; MOVS R0,#3; RSBS R0,R0,#0; ANDS R0,R1` produce different bytes even though the same instructions are present — the LDRB position in the instruction stream matters.
- **Register-reuse between LDRSH offset and BL argument**: When the original uses R2 for both a LDRSH offset and then immediately reuses R2 for a BL argument, pure C needs explicit shaping. `func_08011774` and `func_08014E88` now match with `r1 = *(s16 *)(base + r2)` plus an ABI-shaped function-pointer call; if the compiler swaps operands, pin the base/offset registers and reshape the addition order. Do not add a non-empty instruction shim.
- **Generated-coordinate stack loads (resolved for `func_0800C15C`)**: Non-volatile `s16` locals plus a typed `func_08006F84(s16, s16 *, s16 *)` declaration make agbcc emit the target `SB = SP+0xA` setup and indexed `LDRSH` loads in ordinary C. The former narrow call/load shims are now historical only.

### Bitfield / mask traps
- do not replace bitfield extraction with AND masks when the original is a shift-pair
- `byte &= ~N` is dangerous when `(~N & 0xFF)` fits in 8 bits; agbcc tends to emit `MOVS #imm8; ANDS` instead of `MOVS #N; RSBS; ANDS`
- `-1` / other negative immediates can pick `NEGS`, `RSBS`, or literal-pool forms differently than expected
- some OR/bit-clear forms need a very specific spelling to avoid extra `ADDS`
- **register-pinning workaround**: when `&= ~3` must emit `MOVS R0,#3; RSBS R0,R0,#0; ANDS R0,R1` but agbcc prefers `MOVS #0xFC; ANDS`, pin the mask variable to R0: `register u32 m asm("r0"); val = ptr[7]; m = 3; m = -m; m = val & m; ptr[7] = m;`. **CRITICAL**: load the byte into a separate `val` local BEFORE creating the mask, so the compiler puts LDRB before MOVS/RSBS. Without this, the compiler may put MOVS/NEG before LDRB, producing different byte order.
- **Normalization-before-base-load ordering**: in runtime-record setters where the target begins `LSLS/LSRS R2,#24` before loading the record base from `arg0+0x18`, declaring both values independently lets agbcc hoist the base load ahead of normalization. Register-pinning the incoming value and base is not sufficient by itself; retain the rejected candidate as evidence and add an explicit compiler-visible dependency or a proven empty barrier before retrying. The `func_080F2374`/`080F24A0`/`080F24C0`/`080F253C`/`080F2558`/`080F2578` Round 46 probes demonstrated this ordering trap. Do not use instruction-bearing inline asm to force it.
- **Ordering repair result**: a source-level empty memory barrier (`asm("" ::: "memory")`) after the `LSLS/LSRS #24` pair restored the target order for `func_080F2374`, `func_080F24A0`, `func_080F24C0`, `func_080F2558`, and `func_080F2578`; the barrier contributes no machine instructions. `func_080F253C` still demonstrates the separate constant-folding blocker, so do not treat the family as universally solved.
- **Shift-OR-set instruction interleaving**: for `ptr[N] = (ptr[N] & 0x7F) | (arg0 << 7)`, declare `u32 shifted` as a local variable BEFORE `ptr`, and compute `shifted = arg0 << 7` after loading `ptr`. This forces the compiler to interleave `LSLS R0, #7` between `LDR R3` and `LDRB R2`, matching the original. Writing `arg0 = arg0 << 7` before loading `ptr` puts LSLS before LDR, which is wrong.
- **Forward loop with pointer increment pattern**: for loops that advance a pointer before checking (e.g., iterating through GraphicsTable entries), use `register char *r2 asm("r2")` to pin the pointer register, combined with `goto` labels for loop control. Example:
  ```c
  void func_080024E4(void *arg0) {
      register char *r2 asm("r2") = arg0;
      goto start;
  loop:
      r2 += 0xC;
  start:
  if (*(void **)r2 != NULL)
      goto loop;
  func_0800247C(r2);
  }
  ```
  This preserves the original instruction order (`ADDS R2,#0xC` before `LDR R0,[R2]`) and the specific register allocation the original asm uses.
- **Raw 32-byte record setter**: when the target keeps the byte-normalized value in `r2`, the index shift in `r1`, the first record base in `r3`, and uses `ADDS R3,R1,R3`, pin `r0`–`r3`, compute `r3 = (u8 *)((u32)r1 + (u32)r3)`, then reload the base into `r0` and use `r1 += (u32)r0` for the second store. Example: `func_080F26BC`.
- **BLS vs BLE for loop conditions**: `u32 i; while (i <= 2)` generates `BLS` (unsigned lower-or-same), while `s32 i; while (i <= 2)` generates `BLE` (signed less-or-equal). Check the original's branch type to determine the correct counter type. Using the wrong type produces different bytes even though the loop semantics are identical for non-negative values.
- **LSLS sign-bit test pattern**: When the original tests a specific bit using `LSLS R0, #0x1D; CMP R0, #0; BGE`, write `s32 val = ptr[N]; val = val << 0x1D; if (val >= 0) return; callee();`. Do NOT use `if (val & 4)` — that generates `MOVS R1, #4; ANDS; CMP; BEQ` which is different code.
- **Literal-pool AND mask** — `gGraphicsBuffer.DISPCNT &= 0xEFFF` compound assignment produces the right register allocation. Using a local `u16 val; val = ...; val &= ...; gGraphicsBuffer.DISPCNT = val;` produces wrong register allocation (AND result in R1 instead of R0).
- **Const bit-OR-set**: `gGraphicsBuffer.DISPCNT |= 0x1000` needs a local variable `u16 val; val = gGraphicsBuffer.DISPCNT; val |= 0x80 << 5; gGraphicsBuffer.DISPCNT = val;` to get the right register allocation (OR into R1, not R0).

### Addressing / literal-pool traps
- D_ absolute-address access is known-good mainly at offset `0`; non-zero offsets often drift
- large byte offsets may need pointer shaping so Thumb uses the same split (`+8` then `[+0x1E]`, etc.)
- `((u32*)&gGlobal)[N]` may compile to a symbol+offset literal relocation instead of base-load + offset addressing
- raw relocatable object bytes can mislead; final linked ROM is the real gate
- **Internal local-label function-size quirk**: some target asm places a local branch label before the end of a function without an explicit function-size marker. objdiff may infer a truncated target symbol and report a low per-symbol percentage even when the complete target/candidate `.text` sections are byte-identical. Compare the full section and use the strict linked ROM gate; example: `func_08002024`.
- **Isolated agbcc assembly match ≠ ROM match**: Register allocation differences (e.g., R1 vs R3 for a loop pointer, R0 vs R1 for a STRH target) produce semantically identical but byte-different machine code. The linked ROM is the only reliable gate. Always verify with a full Docker build, not just agbcc output comparison.
- **Literal-pool AND+OR instruction ordering**: For `gGraphicsBuffer.DISPCNT = (DISPCNT & mask) | val`, the C declaration order matters. `loaded = base[0]; mask = 0xFFF8` produces `LDRH R2; LDR R1,=mask` (halfword first then mask), while `mask = 0xFFF8; loaded = base[0]` produces `LDR R1,=mask; LDRH R2` (mask first then halfword). The original typically loads the halfword first, so declare `loaded` before `mask`.
- **LSLS-before-LDR**: For `base + (arg0 << N)`, declare `shifted = arg0 << N` as a local BEFORE loading the base pointer to get `LSLS; LDR` ordering. Loading base first produces `LDR; LSLS`.
- **BICS pattern**: `result = 1; result &= ~val` generates `MOVS R0,#1; BICS R0,R1`. Writing `1 & ~val` or `~val & 1` generates `MVN; AND` instead.
- **Pointer-to-global vs global-value**: `base = &gCurrentSceneData` generates `LDR R4, .literal` (loads address into R4), while `base = gCurrentSceneData` generates `LDR R0, .literal; LDR R4, [R0]` (loads value into R4). Use `&gCurrentSceneData` when the original keeps the global's address in a register and dereferences it multiple times.
- **Argument normalization after a global load**: when the target copies an incoming `u16` into `R1`, loads a global object into `R0`, and only then emits `LSLS`/`LSRS` on `R1`, pin the C argument temporary to `r1` before declaring/loading the global object. Batch 186 used this for `func_080A002C`; the source remains ordinary C with only a register-pinned declaration.

### Repo integration traps
- grouping multiple functions in one C file breaks matches; for batches, still create one `src/decomp/asm_xxxxxxxx.c` per function
- forgetting to move the old asm file causes duplicate / wildcard collisions
- forgetting the linker-script swap breaks standalone TU conversions
- included asm stubs inside an existing C TU (e.g. a mid-file `#include` in `graphics_table.c`) may need the host TU split before a standalone conversion will preserve ROM order
- bare `extern` declarations for g-symbols can fight existing repo types
- C89 declaration ordering still matters in this repo
- **Dirty-worktree false mismatches**: A manual edit left over from a failed attempt can make every later `apply_conversion` look like a candidate ROM mismatch. Before concluding callee-risk, run `git status --short` and revert unrelated edits. Batch 109 confirmed this: a lingering `func_08015A88` signature/call edit in `src/decomp/asm_08012c64.c` caused several false mismatches until it was reverted.
- **Included-stub linked mini-batches can be safe**: when a caller depends on a small callee, convert the callee first, verify ROM OK, then convert the caller in the same chunk. Batch 109 converted `func_08014E88` before its caller `func_080152A0`; both used naked inline asm and preserved ROM identity.

## Specific accepted shaping examples
- gBeatscriptScene second-word access: use `u32 *p = (u32 *)&gBeatscriptScene; p[1]`
- byte decrement siblings: `((u8*)gCurrentSceneVariable)[0xBBA]--`
- multi-store-with-reload pattern:
  - `u8 *p = (u8*)gCurrentSceneVariable;`
  - `*(u16*)(p + X) = 0;`
  - `p[Y] = 1;`
  - `*(u16*)((u8*)gCurrentSceneVariable + Z) = arg0;`
- `gCurrentSceneData` helper pattern:
  - `*(u32*)((u8*)arg0 + off) += *(u16*)((u8*)gCurrentSceneData + 0x16);`
- `sprite_id_delete` byte-offset helper pattern:
  - `sprite_id_delete(gSpriteHandler, *(u32 *)((u8 *)gCurrentSceneVariable + off));`
  - if the original has a pre-call like `func_0800CDB0(1)`, keep it before the delete and keep the byte-cast on the offset load

- **LSRS vs ASRS (logical vs arithmetic shift right)**: To force `LSRS` instead of `ASRS`, cast the intermediate to `u32` before shifting. For example, `(u32)val << 0x17` followed by `>> 0x19` generates `LSLS R0, #0x17; LSRS R3, R0, #0x19` while omitting the cast produces `ASRS`.
- bit-extract via LSLS+LSRS pair: when the original uses `LSLS R0, R0, #N; LSRS R0, R0, #N` to extract a single bit, C `& 1` or `(x << N) >> N` may generate AND or ASRS instead of LSRS. Use inline asm with `.syntax unified` to match exactly:
  ```c
  s32 result;
  asm volatile(
      ".syntax unified\n"
      "ldrb %0, [%1]\n"
      "lsls %0, %0, #31\n"
      "lsrs %0, %0, #31\n"
      ".syntax divided\n"
      : "=r"(result) : "r"(ptr)
  );
  return result;
  ```

### included_stub asm format traps
- included_stub asm files that use `.syntax unified` embedded in a C `asm()` string cannot be assembled by the `compile_and_view_asm` tool (it writes the raw string to a temp `.s` file which the assembler rejects)
- Workaround: manually assemble a standalone `.s` version of the target, compare with `objdump -d` and `objcopy -O binary` + `cmp`, then use `apply_conversion` directly after manual verification
- **include path depth**: for included_stub conversions where the host C file is in a subdirectory (e.g. `src/scenes/main_menu.c`), the include-shim path must use `../decomp/asm_XXXX.c` instead of `decomp/asm_XXXX.c`. The `apply_conversion` tool doesn't account for this and generates wrong include paths for subdirectory host files
- **naked asm with .ltorg**: For `__attribute__((naked))` included_stub functions that reference literal pool constants (e.g., `ldr r1, =D_083A4A80`), add `.balign 4, 0` and `.ltorg` after the epilogue to match the original's literal pool placement. Without `.ltorg`, the assembler may defer the literal pool to the next function, changing the PC-relative offset in the `ldr` instruction and causing a ROM mismatch. Example: `func_0800C080`.

### noreturn trap
- **Do NOT use `__attribute__((noreturn))`** to suppress compiler-generated epilogues — it changes caller codegen and causes cascading ROM mismatch. Even if the function body is pure inline asm with `bx lr`, use plain `void` return type
- For simple BX LR leaf functions, `void func(void) {}` produces `BX LR + NOP` which matches in the final linked ROM even when the original object has `BX LR + .short 0x0000` — the linker resolves the alignment padding correctly
- Object-level NOP differences (`0xC046` vs `0x0000`) do NOT cause ROM-level mismatches for simple leaf functions

### ⛔ New naked inline asm conversions are banned

**Do not add new `__attribute__((naked))` + whole-function inline asm wrappers.** Naked asm wrappers are not real decompilation — they are just the original assembly wrapped in a C function shell, providing zero readability improvement. The goal of this project is to recover readable C, not to re-encode assembly.

**Mandatory C-shaping toolbox.** Keep iterating with these techniques, or switch to another real-C candidate. Do not pivot from a near-miss to naked asm:

1. **Pure C** with correct types, statement ordering, and local variables
2. **Register-pinned variables** (`register type asm("rN")`) to control register allocation
3. **empty asm barriers only** (`asm volatile("" : "+r"(x))`) to prevent instruction reordering or register swaps
4. **empty asm clobbers only** (`asm volatile("" ::: "r1")`) to force specific register choices
5. **Statement reordering** — declaration order affects instruction order in agbcc
6. **Type shaping** — `u16` vs `s32`, `(u32)` casts for LSRS vs ASRS, non-void return type for POP{R1};BX R1 epilogue
7. **Load-base-first trick** — assign global base to a local pointer before computing offsets
8. **Pointer arithmetic shaping** — `(u8 *)base + offset` vs `base[N]` vs pointer-advance patterns
9. **goto labels** for loop control when `for`/`while` generate wrong branch types

**Legacy hard cases that may stay naked until a real-C spelling is found:**
- **`_call_via_r1` / indirect call via register** — C always generates a direct BL; no pure C way has been found to emit `LDR R1,=func; BL _call_via_r1`
- **R2 register-reuse between LDRSH offset and BL argument** — the compiler moves the offset to a different register, which is functionally identical but byte-different
- **s16 callee-signature sign-extension** — when the callee's formal parameter type forces extra sign-extension that the original doesn't have, and the callee is in a different TU that can't be changed

These reasons justify leaving an existing legacy naked file untouched or marking a candidate blocked. They do not justify creating new naked asm progress.

**NOT valid reasons for naked asm:**
- "Register allocation is different" — try register pinning, asm volatile barriers, and statement reordering first
- "I tried for a while and got tired" — there is no attempt-count escape hatch; keep shaping C, pick another real-C candidate, or mark it blocked
- "It's 99% close" — close is not a reason to re-encode the function as naked asm; keep shaping or block with the exact mismatch
- "It's faster" — speed is not the goal; readable C is the goal
- "The function has a 5th stack argument" — use `u32 sp[1]` or similar array-based stack args in pure C

**Converting existing naked asm to real C:** Existing naked asm decomp files should be revisited and converted to real C whenever possible. Priority: (1) functions where naked asm was used only for register allocation differences, (2) functions where naked asm was used for instruction ordering fixable with statement reordering, (3) functions where naked asm was used for a trap that now has a documented C workaround. Each converted function must still achieve 100% match with `compile_and_view_asm` before applying.

**Included-stub helper typedef collision trap:** each `src/decomp/*.c` file is included into the host TU, so generic helper typedef names like `TaskArgs` can collide across files. Prefer unique typedef names or anonymous local structs inside each decomp file.


### Legacy naked-wrapper cleanup wins
- `func_08002468`: use explicit `u32` shift temporaries (`result = *ptr; result <<= 31; result >>= 31;`) instead of `& 1` to force the original `LDRB; LSLS #31; LSRS #31` sequence.
- `func_0800C080`: same family as `func_0800C110`; a local task-argument struct plus non-void `return start_new_task(...)` preserves the stack block and `POP {R1}; BX R1` epilogue without naked asm.
- `func_08001D5C`: register-pinned C can preserve the four halfword matrix stores once the 5th stack arg is loaded into a normal C parameter and all args are shaped as `s32` before explicit `<< 16` / `>> 16` truncation.
- `sprite_set_x`, `sprite_set_y`, `sprite_set_x_y`: use `s32` value parameters to prevent early caller-side truncation, preserve handler in `r5` before truncating value args, and use ordinary C stores after the `sprite_is_invalid` guard. For `sprite_set_x_y`, let the y temporary be an unpinned local so agbcc naturally saves/restores `r7`.
- **Scene sprite-position wrapper**: when a small wrapper passes the scene-data sprite ID at `gCurrentSceneData + 0x2D0` to `sprite_set_x_y`, load `gSpriteHandler` before `gCurrentSceneData`, stage the index as `0xB4 << 2`, use an indexed `s16` load, and normalize widened coordinate arguments with explicit `<< 16` / signed `>> 16` temporaries. Register-bound locals preserve the literal-load and call ABI shape without instruction asm.
- `func_080CD564`: register-pin the destination pointer to `r3`; use separate pinned `r2`/`r1` temporaries so the two word copies match `LDR R2/STR R2` then `LDR R1/STR R1`.
- `func_080EE830`: a normal C function pointer local (`func = func_080efc88; func((void *)r2)`) can reproduce `LDR R1, =func_080efc88; BL _call_via_r1`; use `r0 = r3 + r0` for the command-skip pointer add to preserve operand order.
- `sprite_anim_get_cel_total` / `sprite_get_anim_duration`: declare the helpers as `u32` to avoid callee return sign-extension, then preserve the original caller code by spelling the cel-total assignment as `(sprite_anim_get_cel_total(anim) << 24) >> 24`.
- `func_08011774` / `func_08014E88`: indexed `LDRSH` can be recovered in real C with `r1 = *(s16 *)(base + offset)` when the base/offset registers are pinned and the subsequent call uses a widened ABI-shaped function-pointer typedef. This removed the former narrow load shims.

### Legacy inline-asm removal patterns (2026-08-04)
- **ABI-shaped ordinary calls**: When a header prototype uses `s16`/`s8` or `u16` but the target already has a sign-extended or widened value in an ABI register, call through a unique local function-pointer typedef with `s32`/`u32` parameters. This preserves register reuse while keeping the source as real C. The accepted pass applies this to `sprite_set_visible`, `sprite_set_base_palette`, `sprite_set_anim_cel`, `sprite_set_x_y`, `func_08011504`, `play_sound`, `schedule_function_call`, `sprite_is_invalid`, `__udivsi3`, and related wrappers.
- **Indirect call via R0**: `((void (*)(void))r0)();` is ordinary C and emits the target `_call_via_r0` helper, replacing the old non-empty `bl _call_via_r0` shim.
- **Indexed signed loads**: `r1 = *(s16 *)(base + offset)` recovers the target `LDRSH`; pin `base`/`offset` and use the source-level addition order when the encoding matters. In `func_08012DCC`, `offset += ids` was required to preserve `ADDS R1,R2` rather than the reversed operand encoding.
- **In-place two-operand ADD shaping**: an empty barrier that clobbers condition codes immediately before ordinary `rN += imm` makes agbcc emit the target two-operand form. This removed the shims from both `func_080141C8` (`ADDS R2,#2`) and `func_08014DFC` (`ADDS R5,#4`).
- **Compiler-generated STMIA**: keep a sequential fill pointer as a `u32 *` and write with `*r2++ = r1`; agbcc can select `STMIA R2!,{R1}` for the loop. This removed the former `stm` shim from `func_08015A4C`.


### Indexed large-offset beatscript stores

For stores indexed from `gBeatscriptScene` at a large fixed offset, use a register-pinned base, shift the index before assigning the offset literal, and add in the target's operand order. Empty barriers may separate those stages; do not use instruction-bearing inline asm.

### Nested scene-variable pointer helpers

For tiny helpers that load `gCurrentSceneVariable`, then load a nested pointer at `+0xC`, register-pinned `u8 *` bases preserve the original `LDR` chain and low-register stores. An empty compiler barrier after the loads can keep the shift and field update in the target order; the barrier must contain no instruction text.

### Little-endian byte serialization

For unrolled four-byte readers/writers, use a byte pointer with sequential post-increment for stores, and explicit `LDRB`-equivalent byte loads with shifts/or operations for reads. This can produce exact Thumb without register pins.

### Round 63 additions: interwork returns and scene predicates

- **Interwork epilogue return width**: for an included wrapper whose target ends in `POP {R1}; BX R1`, a widened non-void return such as `u32` can preserve the target epilogue. The same body declared `void` emits `POP {R0}; BX R0` under `-mthumb-interwork`; confirm the result with the isolated object before applying it.
- **Raw ABI call through an existing host-TU declaration**: when an included callee already has a narrower or conflicting declaration, do not redeclare it. Cast the existing symbol to a unique local function-pointer typedef with the target's widened `s32`/`u32` register ABI, and call through that pointer. This keeps the source as ordinary C and avoids a same-TU prototype conflict.
- **Scene-data byte predicate siblings**: `if (((u8 *)gCurrentSceneData)[0x173] == 1) callee();` reproduces the compact `LDR`/`LDRB`/compare/conditional-call wrapper family. Keep the byte cast and direct equality spelling; use the sibling's callee declaration rather than introducing a struct field model.

### Round 64 additions: scene-slot stride setters

- **Raw scene-slot stride family**: for targets that load `*(u8 **)((u8 *)gCurrentSceneVariable + 0x10)`, write one field, add `0x20`, and loop through seven entries, use a `u8 *` pointer plus a `u32` counter initialized before a `do { ... } while (count <= 6)` loop. This preserves the target's low-register pointer, `R2` value copy, and `BLS` loop shape in ordinary C. The word-field variants at offsets `0x0C`, `0x14`, and `0x18` matched with this spelling.
- **Post-call key-test wrapper**: after a preceding void call, `func_08009EE4(((u16)gCurrentKeys >> 8) & 1);` preserves the target's `LDRH`/`LSRS`/`MOVS`/`ANDS` tail. Keep the explicit `u16` cast when the host headers use a wider key declaration.
- **Sprite visibility wrapper ABI**: `sprite_id_set_visible(gSpriteHandler, get_current_mem_id(), 0);` naturally preserves the handler in `R4`, copies the returned memory ID into `R1`, and materializes the zero third argument in `R2` for the short wrapper family.
- **Numeric ROM-address fallback**: if a candidate's `D_08xxxxxx` symbol is known to the assembly include but missing from `undefined_syms.ld`, test the equivalent numeric absolute address in isolation. Round 64b proved this can produce exact linked bytes and a byte-identical ROM for `func_080B0760`/`func_080ED380`; use it only with both gates passing, because numeric constants can otherwise lose required relocation identity.

### Round 66 additions: scene-variable leaves and indexed stores

- **Staged indexed scene store**: for a target that shifts an incoming index before adding a fixed scene offset, keep the shift in a separate unsigned local, load the scene base first, then add the fixed offset and shifted index in the target order. `func_080D2450` matched with `arg0 << 24`, later `>> 22`, and a staged `base += 0x3AC` sequence.
- **Byte/bit scene leaves**: a `u8 *` base local plus explicit byte-pointer arithmetic preserves compact low-register `LDRB`/`STRB` and mask/OR leaves. Numeric shift expressions such as `(0xFF << 1)` can be retained when they reproduce the target's immediate calculation.
- **Hardware register read as ordinary C**: a `volatile` typed pointer to `0x04000100` is a normal memory access and can match a hardware-register load; it is distinct from volatile inline asm and passes the no-instruction-asm policy.
- **Post-call offset store trap**: the natural `func(); *(u16 *)(base + (0xDF << 2)) = 1;` spelling for `func_080D3A60` remains a near miss. Keep the candidate and its receipt as evidence until an isolated permutation reproduces the target `MOVS`/shift/address ordering; do not waive it merely because the semantics are obvious.

### Round 67 additions: wrapper branch and literal shaping

- **Mask-negation folding repair**: when a target requires `MOVS #3; RSBS` rather than agbcc's folded `SUB #4`, keep the mask in a named local, insert an empty nonvolatile `"+r"` compiler constraint, then negate it in the next statement. This emits no asm instructions; `func_0800CDB0` is the new standalone proof point.
- **Fall-through branch spelling**: if the target branches directly to the false return and places the mutating path in fall-through order, write `if (predicate != 0) return 0; mutate(); return 1;` rather than the semantically equivalent `if (predicate == 0) { mutate(); return 1; }`. `func_08015E24` changed from `BEQ` to the target `BNE` layout this way.
- **Thumb callback addend**: a callback passed as data may require the function address's Thumb `+1` addend in the literal pool. An explicit ordinary-C cast such as `(void (*)(u32))(set_pause_beatscript_scene + 1)` preserves that relocation/addend; omitting it can leave all instructions equal while changing a pool byte, as Round 67's `func_080166E4` screen demonstrated.

### Round 68 additions: predicate polarity and typed copy loops

- **Boolean fall-through polarity**: when the target uses `CMP; BEQ; MOVS #1; B; MOVS #0`, write the equality case as the early return (`if (value == 0) return 1; return 0;`). The semantically equivalent inverted test emits `BNE` with the return constants swapped; `func_080D74D0` was a 99.53333% near miss until this source-level shape was corrected.
- **Project typedef header order**: `include/types.h` declares structs and aliases that assume the typedefs from `global.h`. Standalone isolation candidates that use `u8`/`u16`/`s32` and include `types.h` must include `global.h` first; otherwise agbcc reports a misleading cascade of struct/type errors. The `func_080721A0`/`func_080721BC` loop pair is the current proof.
- **Typed byte/halfword copy loops**: a check-before-loop C spelling with typed pointers and a widened `s32` count reproduces the target's `PUSH {LR}`, low-register destination copy, post-increment, decrement, and `BNE` sequence for both byte and halfword strides. Keep the source and destination pointer increments as separate statements when preserving the target's register order.
- **Reloaded scene-data wrapper arguments**: direct raw-byte-pointer expressions can preserve a target that computes an output pointer or field pointer first, then loads the scene-data halfword as the later call argument. The three Round 68 scene wrappers demonstrate this without a linker-map addition by using the proven numeric ROM address for `D_083FBAA4`.

### Round 69 additions: scene update ordering

- **Predicate return polarity**: semantically identical boolean leaves can require opposite source tests. For a target `CMP; BEQ; MOVS #1; B; MOVS #0`, use `if (value == 0) return 1; return 0;`; for `CMP; BNE; MOVS #0; B; MOVS #1`, use `if (value != 0) return 0; return 1;`. `080A7A74` and `080D25C4` are paired proof points.
- **Three-operand add spelling**: if the target has `ADDS R0,R2,R0` after loading an accumulated value, `r0 = r2 + r0;` preserves the three-operand form; `r0 += r2;` can emit the shorter `ADD R0,R2`. `func_080526D0` needed the explicit source order.
- **Scene-data result/factor pins**: for `MULS R0,R1,R0` followed by a large-offset store, bind the incoming result to R0, the shifted scene-data factor to R1, and the scene-variable base to R2. `func_0809E804` matches with real C and no instruction asm.
- **Post-compare indexed byte fallback**: if the target branches to a constant fallback before loading the global base, write `if (arg > limit) return fallback;` and assign the base pointer only in the remaining path. Declaring the pointer at function scope but initializing it before the test hoists its literal load and reverses the branch layout; `func_0805F08C` is the exact example.
- **R1 indexed-halfword reuse trap**: a target that uses one register for the global-pointer literal, indexed `LDRSH` offset, and later accumulator may remain a near miss even with hard-register locals and empty constraints. Keep `func_080DD8A4` as evidence until the source reproduces `LDRSH [R2,R1]`; do not accept the R0-indexed variant merely because all stores and semantics are correct.

### Round 70 additions: threshold branches and difference ordering

- **Equality-return branch layout**: when the target compares two bytes and branches with `BEQ` to a later `MOVS #1`, write the equality case as the early return (`if (a == b) return 1; return 0;`). The inverted `!=` spelling is semantically identical but makes agbcc emit `BNE` with the return blocks reversed; `func_0806EC7C` is the exact proof.
- **Unsigned threshold fall-through**: when the target uses `CMP; BLS` to a later constant return, write the less-than-or-equal case as the early return (`if (value <= limit) return constant; return fallback;`). Writing `if (value > limit) return fallback;` produces `BHI` to the other block and is a near miss even when the predicate is equivalent; `func_0809C0C0` demonstrates the repair.
- **Difference load order**: for a helper whose target loads the argument field first, then the global-scene field, subtracts into the result register, and compares that result with the second argument, pin the incoming values to the target ABI registers and spell the loads/subtraction as separate C statements. `func_08089648` matches with register-bound locals and no instruction asm.
- **Asmlift exact skeletons still need project validation**: `func_0805C5D8` and `func_08088B80` were exact from the first asmlift-shaped C spelling, but Round 70 still screened all five candidates through the same Docker isolation receipt. Semantic similarity is a discovery aid; only exact isolated code followed by the full ROM gate is admissible.

### Round 79 additions: scene animation helpers

- **Scene-state store/load register roles**: the final `func_0801646C` spelling uses ordinary locals plus small inner C blocks and still reproduces the target `MOVS/LSLS/ADDS` forms without any asm pins. The earlier pin-assisted spelling was byte-exact but is retained only as historical evidence; the no-pin candidate is the accepted source-quality form.
- **Widened countdown zero test**: load a halfword into `u32`, decrement it, store the widened value through the halfword lvalue, then test `if ((value << 16) == 0)`. This preserves the target's `STRH` followed by `LSLS #16` rather than inserting a pre-store `LSRS` for a `u16` temporary.
- **Sprite call load order**: assign `handler = gSpriteHandler` before deriving the scene-data sprite ID, then call `sprite_set_visible`/`sprite_set_anim_cel` through that local. This keeps the handler literal load before the scene-pool load.
- **Packed scene offsets need evidence, not denial**: the typed `MainMenuSceneData` experiment changed the generated field width/indexing and remained a real near miss. Keep the byte-accurate raw view when the layout is not yet modeled, and let the strict source audit expose each cast/offset for later struct recovery.

### Round 78 additions: counted save-unlock loops and aggregates

- **Save-buffer microgame count loop**: use `u8 *flags = gSaveBuffer->microgameFlags`, initialize a `u32` count and index, then use a `do { if ((flags[i] & mask) != 0) count++; i++; } while (i <= 0xE1);`. This preserves the target's `BLS` loop condition, byte indexing, and callee-save register allocation for the 0x100-byte flag range.
- **Counted unlock threshold**: keep the post-loop test as `if (count > 0xD4)` so agbcc emits the target's `CMP #0xD4; BLS` fall-through shape before the unlock side effect.
- **Long aggregate OR wrapper**: for a caller that repeatedly ORs no-argument `s32` results, use one mutable `s32 result` and separate `result |= callee();` statements. This retains the target's R4 accumulator and repeated `ORRS` sequence even when several callees are already converted C functions.

### Round 77 additions: richer save-unlock predicates

- **Nonzero-to-one prerequisite count**: when the target turns a helper result into a boolean with `RSBS`/`ORRS`/shift, preserve `u32 count = (u32)((0 - temp) | temp) >> 31;` rather than using a direct C comparison. This retains the target's normalization before later count increments.
- **Count thresholds**: for sibling unlocks that require more than one completed prerequisite, keep `u32 count` and write `if (count > 1)`; the direct boolean spelling can change the compare/branch shape.
- **Threshold-ready temporary**: when a helper threshold feeds a later unlock branch, use `s32 ready = 0; if (func_08008AA4(stage) > 0xE) ready = 1; if (ready != 0) ...;` to preserve the target's temporary and branch placement. Round 77 matched this shape in the stage-C/D predicates.

### Round 76 additions: save-unlock predicate wrappers

- **Nested save-unlock predicates**: for wrappers that test `save_is_stage_unlocked(id) == 0`, perform a second progress test inside a nested C `if`, then call the unlock helper and return the target flag. This preserves the target's `BNE`/`BEQ` failure layout across the fifteen Batch 225 siblings.
- **Scalar-return interwork epilogue**: these semantically flag-valued wrappers use `s32` return types and an explicit zero fallback, preserving the target `POP {R1}; BX R1` epilogue. Do not make them `void` or rely on an implicit return.
- **Shifted flag constants**: when the target emits `MOVS R0, #0x80` followed by `LSLS R0, R0, #N`, write the result as `0x80 << N` rather than a precomputed literal. This preserved the target literal/shift shape in `func_080160C8`, `func_080160F0`, `func_08016118`, and the later achievement wrappers.
- **Threshold branch spelling**: for `CMP value, #limit; BLS failure`, use `if (value > limit)` for the success body. This keeps the failure branch as the target's fall-through-compatible unsigned threshold test in the `080160C8`/`080160F0`/`08016118` siblings.

### Round 75 additions: task/scene/sprite wrappers and ABI placeholders

- **Task-loader callback wrappers**: short ordinary-C wrappers can preserve the target's task setup and Thumb callback literal when the callback is explicitly cast and the existing task helper prototypes are declared. `func_08016E9C` and `func_08016EC8` use the numeric ROM table addresses and `(callback + 1)` callback expressions directly; the isolated literal-pool bytes are the authority.
- **Scene-table wrapper fan-in**: for scene helpers that call `func_080DF224` or `func_080DF28C`, keep a local `u8 *base`, spell scaled offsets such as `(0x86 << 1)` explicitly, and pass the numeric ROM table address as an ordinary pointer. `func_0803E9A0`, `func_0803E9C4`, and `func_0803E9E8` are exact examples.
- **Ignored middle ABI parameter**: if the target consumes its third pointer from R2 but the logical C operation only appears to have two useful inputs, retain an unused middle parameter in the function declaration. `func_0806A958` and `func_0806A97C` were near misses with two parameters and exact after adding the ignored `s32` parameter; this is ABI shaping, not an asm workaround.
- **Scene-thread plus sprite visibility**: `scene_set_current_thread(1)` followed by `sprite_set_visible` and raw signed-halfword scene-record offsets remains a productive ordinary-C family. `func_0806A958`, `func_0806A97C`, `func_080EC55C`, `func_080EC62C`, and `func_0803E244` provide offset and handler-global variants.
- **Conditional scene-data sound wrapper**: when a byte at `gCurrentSceneData + 0x173` controls a direct sound call, include `scenes.h` for the project global and use a byte-pointer access with an explicit `== 1` test. `func_080EB1F4` matches without inline asm.

### Round 74 additions: wrapper batches and register-role repair

- **Multi-call wrapper fan-in**: a small ordinary-C batch can preserve several direct `BL` calls when each callee's ABI is declared explicitly. `func_08022650` keeps three zero-argument calls ahead of the `gCurrentKeys` byte test, while `func_0803292C` keeps the incoming argument in R4 across four calls.
- **Saved scene pointer across a call**: for `func_08024F68`, initialize a local byte pointer from `D_03006524` before `scene_set_current_thread(0)`, then read the `+0x50` field after the call. This preserves the target's R4 lifetime; reloading the global after the call changes the stream.
- **Scene-variable accumulate/store**: `((u8 *)gCurrentSceneVariable + (0xE7 << 3))` plus a separate load/add/store reproduces the target's scaled offset and R1/R2 order for `func_08072700`.
- **Graphics-buffer register roles**: when a target loads the graphics base into R1, the halfword into R2, and a mask into R0 before zeroing R2 for two later stores, bind those roles in ordinary-C locals. `func_08039A44` is exact with register declarations and no instruction text.
- **R4 table base across RNG**: a numeric ROM table pointer may be initialized in a register-bound R4 local, but agbcc can legally delay the load until after the RNG call. An empty nonvolatile `"+r"` constraint keeps the base live without emitting an instruction; `func_0801A688` is the proof.
- **Header-local candidate context**: a project header can introduce unrelated `-Werror` warnings into an isolated candidate. For `func_08022650`, direct declarations for the needed symbols were safer than including the broad beatscript header; for `func_080733AC`, `scenes.h` was required to declare `gCurrentSceneData`.

### Round 73 additions: scene-data callers and literal-pool accounting

- **Scene-data caller pair**: when a short wrapper loads `gCurrentSceneData` or `gCurrentSceneVariable`, derives a pointer field and a signed halfword at separate offsets, then calls an already-converted helper, use a raw `u8 *` base and explicit byte offsets. `func_08016B14` and `func_080241E8` both matched with ordinary C; the full-context gate is still required because the callee is already C.
- **Literal-pool near miss is a real mismatch**: a numeric absolute pointer can reproduce every instruction while dropping the target's absolute table-address word from the pool. `func_080047D4`'s seven instructions matched, but its candidate lacked the target pool word; this is not eligible for the metadata-only boundary exception. A symbolic linker-map definition or an otherwise ordinary-C spelling that forces the relocation must reproduce the complete `.text` bytes before admission.

### Round 72 additions: absolute indirect calls and audio/data wrappers

- **Absolute indirect-call wrapper**: when the target loads an IWRAM function pointer from `D_03003FEC`, calls it with zero, and returns through `_call_via_r1`, a numeric pointer expression such as `((void (*)(u32))*(u32 *)0x03003FEC)(0);` can preserve the target literal and `u32` no-return epilogue without adding a linker-map symbol. If linked objdiff stops at the local literal-pool boundary, audit the complete linked `.text` before considering the metadata-only force path.
- **Callee result and saved-register ordering**: for `func_080E1F48`'s `LDR R4; BL; ADDS R0,R4`, declare the callee as returning `u32`, pin the table base to `R4`, and use an empty `"+r"` compiler constraint before the call when necessary to keep the load live. The constraint emits no instruction; it is compiler metadata, not an instruction shim.
- **Project prototype beats generic extern**: use `src/audio.h` for `play_sound` in `func_08023494`; a generic or conflicting declaration can change argument normalization and fail an otherwise direct two-call wrapper. The existing project prototype produced the exact `R4` song-base call sequence.
- **Boundary audit remains a separate proof**: Round 72's `func_08003DE0` linked objdiff score was a metadata near miss, but its full 20-byte linked `.text` section matched the target with equal SHA-256. Keep the audit receipt and require the clean full-ROM gate; never promote an ordinary instruction near miss through `--force`.

## Families still worth mining heavily
- conditional byte-check + BL wrappers
- shift-offset store wrappers
- remaining `sprite_id_delete` siblings (NOTE: loop-based variants like asm_08016d3c, asm_0806843c, asm_0806fe20 failed to match; their loop iteration patterns and register allocation don't align with simple C for-loops even when semantically correct)
- `scene_set_current_thread(1)` sibling families
- more MOVS R0, #const + BL wrapper families
- multi-BL wrappers whose good spellings are now documented

### Round 105 additions: sprite-create wrapper and the BL-displacement isolation artifact

- **Sprite-create wrapper**: `func_0800DE84` shows the standard shape for `sprite_create` wrappers — zero-extend both u16 parameters first (the target's `LSLS/LSRS #16` pairs), sign-extend to s16 only at the call site, pass the fixed args (`0x800`, zeros) in source order, then wrap the returned id for visibility and return it. A small named struct (`unk8` at +8) models the scene-data field read that feeds the animation helper.
- **BL-displacement isolation artifact is provable, not guessable**: when an included-stub candidate's only isolate mismatches are the two `BL` words, the cause is the harness linking the candidate alone with `--unresolved-symbols=ignore-all`; callees land at different addresses than in the target-TU link. Prove equivalence mechanically: assemble the extracted target `.s` and the candidate into separate objects, link each with identical `--defsym` callee addresses, and compare the function's bytes. Equal bytes justify the documented metadata-only `--force` admission; the full Docker ROM gate (`rom_exact`) remains the real acceptance control.

### Round 106 additions: scene-tick counter loop (`func_08011DFC`)
- **Once-code between entry test and body**: when the target emits setup instructions (anchor copy, zero init) once, after the `i=0; cmp; bhs` test and with the back edge returning to the body start, use a pre-tested do-while: `i = 0; if (i < count) { setup; do { ... i++; } while (i < count); }`. A plain `for` loop re-emits per-iteration statements on every pass.
- **Anchor via CSE-reused pool load**: declare a typed address local (`struct T **pp;`) and assign it after the initial checks (`pp = (struct T **)&gCurrentSceneData;`). agbcc reuses the r1 pool copy from the initial global read (`ADDS R5,R1,#0`) instead of emitting a second literal-pool load. Declaring the local with an initializer at function top instead loads the pool straight into its home register (`LDR R5,.literal`), which does not match.
- **Nothing pointer-live across indirect calls**: keep every post-call access as a direct `(*pp)->field` expression so agbcc reloads `[r5]` fresh; assigning the pointee to one persistent pointer variable makes it live across the call, pushes allocation into high registers (r8/r9), and shifts every loop register.
- **Immediate byte store from pre-increment**: `if (++p->field >= limit)` stores the incremented byte before computing the comparison's right-hand side address chain; a separate `v = p->field + 1; p->field = v;` spelling lets agbcc defer the `STRB` past the limit-address computation.
- The isolate harness scores such candidates near 0.1 solely from `_call_via_r1` displacement; prove equivalence with the defsym-linked byte audit before using apply `--force`.

### Round 107 additions: three main-menu included stubs and host-TU declaration hygiene

- **Digit-sprite loop overlay**: `func_08012AE8` matches with a named scene overlay containing the sprite-ID table at `+0x9C`, a `u32` value/index loop, and direct `sprite_set_anim_cel`/`sprite_set_visible` calls. The `while (1)` body with the divide-before-increment tail preserves the target's entry fall-through and `BLS` back edge.
- **Scene cleanup overlay**: `func_08014CF8` matches with a named scene overlay for `unk4`, `unk16C`, and the `+0xDE` flags byte. The candidate must use the host TU's existing `func_0800A240` and `get_current_mem_id` declarations, and shared ROM objects such as `D_083A4A1C` must retain their existing array/scalar declaration shape.
- **Boundary proof for BL-pair wrappers**: `func_08011D0C`, `func_08012AE8`, and `func_08014CF8` each had only unresolved-callee BL displacement differences in the candidate-only isolation link. Their complete raw function text/pool bytes matched the host target object; the clean host-TU Docker gate is the final acceptance authority. Do not force a genuine instruction or pool-byte mismatch through this path.
- **Rejected sibling evidence**: `func_080136F4` remains blocked by the documented R7/RSBS code shape and its offset-heavy candidate fails the strict layout audit; `func_08014208` remains blocked because agbcc folds the required `MOVS #5; RSBS` mask into an immediate. Neither was admitted.

### Round 108 additions: natural callee-saved table bases without register pins

- **Table base live across a call**: `func_0801911C`, `func_0801913C`, and `func_0801915C` each assign a typed table base to an ordinary local before calling `get_random_range`, scale the returned index afterward, and call the selected song/sound function. agbcc naturally keeps the live base in `R4`, reproducing the target's `PUSH {R4, LR}`/`LDR R4` sequence without `register ... asm("r4")`.
- **Declaration order is part of the tested spelling**: split the base and offset declarations, assign the base before the RNG call, then assign the offset from the call result. This preserves the target's load-before-call lifetime while avoiding compiler-only register shaping.
- **Screening implication**: already-matched C files can still be useful cleanup candidates. The report count need not move when the function was C-linked already; source-quality residue and reproducible ordinary-C provenance are the progress metric. Keep the full ROM SHA gate as the acceptance authority.

### Round 109 additions: ordinary locals across host-TU and standalone wrappers

- **Saved argument across a call**: `func_08001B04` does not need a pinned `saved` value. Assigning `arg0` to an ordinary local before `func_08001AC0`, then using that local after the call, makes agbcc choose the target's callee-saved `R4` lifetime naturally when the function is compiled inside `code_08001a70.c`.
- **Call-argument home from ABI position**: `func_08019644` reproduces the target's `R2` value argument with an ordinary `u32 value` local; the fixed `sprite_set_visible` prototype and argument order provide the needed ABI placement. `func_080F1574` likewise reproduces its `R1` table base and `R0` offset with ordinary locals and declaration-order assignments.
- **Host-TU proof matters**: for an included function such as `func_08001B04`, compare against the built host object rather than treating its converted wrapper text as a standalone assembly target. The source-only cleanup is accepted only after the integrated ROM gate remains byte-identical.

### Round 110 additions: barrier-free scene, counter, and heap wrappers

- **Scene-variable flag update**: `func_0801B174` matches without the memory barrier when a named scene overlay models the `+0x19`, `+0xEE`, and `+0xF0` fields and the byte flag is copied through an explicit `u32 flags` local. The overlay and local preserve the target's final `LDRB; MOVS #2; ORR` register roles.
- **Fixed-IWRAM fixed-point reload**: `func_080F282C` matches with a typed `u16 *counter` at `0x03000E78`, separate `field`/`factor` locals for the multiply/add sequence, and a `u16 reload` local for the post-store read. Reusing `factor` for the reload changes the allocator; the reload type and separate local are part of the exact spelling.
- **Heap record copier**: `func_08004BD4` matches with a named four-word record overlay and an explicit return of the allocated record pointer. The return statement is semantically honest and preserves the target's natural R0 result without a compiler register pin.
- **Graphics barrier remains evidence-only**: the named overlay for `func_0801F698` passed the strict layout audit but still allocates the base/field/value/result homes differently from the target. Keep its empty `"+r"` constraint until an ordinary-C spelling reproduces the R3-to-R0 copy; do not promote the 50.076923 near miss.

### Round 119/121 additions: arithmetic and heap pin cleanup

- **Two-local fixed-point arithmetic**: `func_080F28F8` removes its R0 pin when `result` is initialized from the parameter, shifted by three, copied into `value` for the half-shift, then added back into `result`. The declaration/assignment order preserves the target's `R1` accumulator, `R0` half-value, and interwork-safe return epilogue.
- **Heap copier sibling**: `func_0800557C` uses the same named four-word record overlay and explicit `(u32)dst` return as `func_08004BD4`; both siblings emit the target's natural R4 source lifetime and R0 return without a pin.
- **Remaining one-pin scheduling traps**: ordinary C for `func_080A002C` hoists the u16 argument normalization ahead of the target's player-global load, `func_080195E4` materializes zero before the target's first pointer add even with a named overlay, and `func_0801D4B4` swaps the incoming value/pointer homes. Keep those as evidence until a genuinely exact source shape is found; do not use a barrier or pin to close them.

### Round 122/123 additions: named-overlay mask/RMW siblings

- **Mask/RMW register shaping**: `func_0801AF18` and `func_0801BEA8` match when a named `+0x18` scene field is loaded into `value`, `mask` is initialized to `0x3D`, negated and ANDed, then ORed with the sibling-specific constant before the store. `func_0801B3E4` uses the same spelling at `+0xF4` with mask `2` and no OR step.
- **Overlay quality matters**: the first raw `u8 *data` spellings were correctly rejected because the alias was used at two numeric offsets. A small named overlay made the layout explicit and preserved the exact target code without relaxing the strict source gate.

### Round 124 additions: serializer, sprite-wrapper, and clamp cleanup

- **Little-endian byte serializer**: for `func_08003998`, load `*cursor` into an ordinary `u8 *p`, perform four `*p++ = value >> shift` stores, then write `p` back to `*cursor`. The pointer's live range naturally selects the target cursor register and the compiler supplies the shifted byte temporary without a pin.
- **Global-load wrapper order**: `func_0801E44C` keeps the literal-pool data base and `gSpriteHandler` in separate ordinary locals before the signed-halfword read and `sprite_set_visible` call. The declaration/initialization order preserves the target's `R1` data base and `R0` handler homes without compiler metadata.
- **Clamp branch layout**: the existing `if (temp <= 0x3F) return 0x7F;` spelling for `func_080F1FB4` remains exact when `value` and `temp` are ordinary `u32` locals; using a narrow `u8 temp` changes the arithmetic stream and is a near miss.
- **Cleanup screening boundary**: the same screen correctly rejected raw multi-offset pointer aliases for `func_080029D0` and offset-heavy allocation candidates for `func_08007FC0`; exact code generation alone is not enough when the candidate fails the strict source-quality gate.

### Round 126 additions: ordinary beatscript field helpers

- **Mapped-base plus literal offset**: `func_0800CAA4` and `func_0800CAB8` match when an ordinary `u8 *base` points at `&gBeatscriptScene`, a separate `u32 offset` holds `0x1C32` or `0x1C30`, and the typed halfword store uses `base + offset`. This preserves the target's two literal loads and `ADDS` without an empty barrier.
- **Scaled index after a mapped base**: `func_0800D224` keeps `base`, `offset`, and `field` as ordinary locals, shifts the incoming index before adding `0x1C5C` to the mapped base, then stores through the resulting word pointer. Declaration order reproduces the target's `R2` base, `R0` scaled index, and `R3` field literal homes.
- **Bounded layout evidence is enough when bounded**: each accepted helper uses two raw pointer accesses and at most one numeric offset, which stays within the strict audit's visible evidence threshold. The old register pins and barriers were compiler metadata, not semantic layout recovery.

### Round 128 additions: nested records and typed field addresses

- **Nested scene record cleanup**: `func_0801D4A0` matches when the `gCurrentSceneVariable + 0xC` pointer is represented by a named scene record, and its target halfword/byte fields are represented by a small named data record. The field assignments preserve the target's R1 scene, R2 data, R0 shift, and R1 zero lifetimes without raw scalar-pointer aliases.
- **Named field address without a pin**: `func_0805CB5C` keeps the graphics field at `0x54` visible in a named overlay, takes its typed address, then assigns the literal through an ordinary value local. Taking the field address produces the target's base-add/literal-load sequence without an empty compiler barrier.
- **Runtime-table base/offset locals**: `func_080F0DE0` needs only an ordinary `base` local from `D_030068E8` and an `offset` local initialized from the argument. The shift/add/store sequence remains exact with bounded two-access evidence.

### Round 129 additions: host-object screening and natural scene wrappers

- **Already-converted included stubs**: when a host TU already includes `src/decomp/asm_*.c` and the original wrapper has been moved to `asm/converted/`, screen against the built host object rather than trying to assemble the archival `asm(...)` wrapper text. The accepted source change needs no linker swap or assembly move; the full host-TU ROM gate remains authoritative.
- **Named fixed-stride table cursor**: `func_080024E4` matches with a named record whose first field is the sentinel pointer and whose trailing padding makes the record 12 bytes. Incrementing the typed record pointer naturally emits the target `ADDS cursor,#0xC` without a raw multi-offset alias or register pin.
- **Ordinary scene predicate plus saved result**: `func_08016E6C` and `func_08016D88` reproduce their target literal loads and callee-saved result lifetimes with ordinary `if` statements and a `u32 result` local. A typed `s16` declaration for the halfword scene flag preserves `LDRSH`; no address-cast or compiler register home is needed.

### Round 130 additions: typed table overlays and host-boundary proof

- **Scene-ID table lookup**: `func_08012DCC` matches with a small named scene overlay whose `s16 *ids` field is indexed by an ordinary `u32` loop local. Keeping the handler, scene, IDs, and ID locals in that declaration/assignment order removes the legacy pins while preserving the target loop.
- **Typed language/scene table lookup**: `func_08014374` uses named one-entry table overlays for the language entry, scene table, and scene byte index. An ordinary `u32 offset` local followed by `offset += (u32)scene_table` preserves the target `ADDS R1,R1,R0`; a direct indexed expression changes the operand homes. Match the host TU's existing `func_08015A88(void)` declaration when applying an included stub.
- **Named scene-data predicate/result**: `func_080166AC` uses a signed `D_030035E0` declaration and a small named scene-data overlay for the `+0x38` halfword. Ordinary conditionals and a saved result retain the target `LDRSH` and callee-saved result flow without pins.
- **Host-object boundary artifact**: an included-stub candidate can be instruction-identical to the built host object while an isolated candidate-only link reports a symbol-size/pool-boundary near miss. Keep the screen receipt and raw target-object comparison, but require the integrated host-TU ROM gate; a genuine register-home difference such as `func_08002514` is not eligible for this exception.

### Round 131 additions: named zero stores and delayed argument normalization

- **Sibling scene-data store**: `func_0801D4B4` matches the `func_0801D4A0` overlay with `data->value = value << 8; data->flag3 = 1;`. The ordinary named fields naturally keep the scene-data pointer in R1 while the shifted input remains in R0, removing both empty barriers and the pointer/value pins.
- **Post-global u16 normalization**: `func_080A002C` needs `u32 value = arg0;`, then the `gBeatscriptScene.musicPlayer` local, then `value = (u16)value;` immediately before the call. This preserves the target's `ADDS R1,R0`, global-player load, and `LSLS/LSRS` sequence without a hard register declaration.
- **Named non-encodable halfword offsets**: for `func_080195E4`, define small overlays with fields at `0x66` and `0x4C` rather than reusing one scalar byte pointer at multiple numeric offsets. Direct named fields keep both `ADDS` address formations and the shared zero store while satisfying the layout-quality gate.

### Round 132 additions: included-stub mask setter overlays

- **One-pin scene byte mask setters**: `func_0800A3BC`, `func_080121B8`, and `func_08013114` match with a small named overlay for the target byte, then ordinary `value` and `mask` locals in the order `value = field; mask = constant; mask = -mask; mask &= value; field = mask;`. This preserves the target's low-register `LDRB; MOVS; RSBS; ANDS; STRB` roles without a hard register declaration.
- **Host-object screen rule**: these already-included stubs show the recurring candidate-only pool/symbol-boundary near miss. Compare the raw instruction body to the built host object and require the integrated host-TU ROM gate; do not treat the isolated percentage as a real mismatch when the body is identical.

### Round 133 additions: adjacent main-menu mask wrappers

- **Post-call scene mask setter**: `func_080109CC`, `func_080144BC`, and `func_08014A0C` use a small named scene overlay for the target byte, then ordinary `value` and `mask` locals in the order `value = field; mask = constant; mask = -mask; mask &= value; field = mask;`. This preserves the target's `LDRB; MOVS; RSBS; ANDS; STRB` sequence across the existing scene/thread call without compiler register homes.
- **Candidate header hygiene**: when a broad project header introduces an unrelated standalone warning, an explicit function extern is sufficient for the isolated candidate if it preserves the same target interface; do not add compiler shaping to silence the warning.
- **External-call host boundary**: candidate-only links can show BL relocation and literal-pool/symbol-boundary near misses even when the function instruction body matches the host object. Keep the receipt and use the integrated host-TU ROM gate; a real instruction mismatch still fails acceptance.
